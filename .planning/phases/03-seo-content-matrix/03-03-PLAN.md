---
phase: 03-seo-content-matrix
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/content/zodiac.ts
  - src/components/zodiac/ZodiacIndex.tsx
  - src/components/zodiac/ZodiacHub.tsx
  - src/components/zodiac/ZodiacYearTable.tsx
  - src/components/zodiac/ZodiacCompatibility.tsx
  - src/app/[locale]/zodiac/page.tsx
  - src/app/[locale]/zodiac/[animal]/page.tsx
  - src/app/[locale]/zodiac/[animal]/[slug]/page.tsx
  - tests/seo/zodiac-content.test.ts
  - tests/seo/zodiac-routes.test.ts
autonomous: true
requirements:
  - SEO-03
  - SEO-08
  - SEO-09

must_haves:
  truths:
    - "D-16 zodiac uses a two-layer structure: `/zodiac/{animal}` tool-like hub plus separate long-tail article pages"
    - "D-17 zodiac article MVP includes 2-3 fixed articles per animal, roughly 24-36 total articles"
    - "D-18 zodiac article writing is source-synthesized from collected and cross-checked material, not invented from scratch or copied from a single source"
    - "D-09 zodiac long body copy uses Simplified canonical copy with OpenCC Traditional conversion, while SEO title/description/H1 can be locale-specific"
    - "D-11 every zodiac hub and article has unique main copy, visible FAQ, and internal links"
  artifacts:
    - path: "src/lib/content/zodiac.ts"
      provides: "12 zodiac hubs and article registry"
      contains: "zodiacAnimals"
    - path: "src/app/[locale]/zodiac/[animal]/page.tsx"
      provides: "Zodiac hub route"
      contains: "generateStaticParams"
    - path: "src/app/[locale]/zodiac/[animal]/[slug]/page.tsx"
      provides: "Zodiac article route"
      contains: "buildArticleJsonLd"
  key_links:
    - from: "src/app/[locale]/zodiac/[animal]/page.tsx"
      to: "src/lib/content/zodiac.ts"
      via: "hub content lookup"
      pattern: "getZodiacProfile"
    - from: "src/app/[locale]/zodiac/[animal]/[slug]/page.tsx"
      to: "src/components/seo/ArticleLayout.tsx"
      via: "article rendering"
      pattern: "ArticleLayout"
---

<objective>
Build zodiac hub and article pages: `/zodiac`, `/zodiac/{animal}`, and `/zodiac/{animal}/{slug}` with 12 SSG hubs, 2-3 source-synthesized articles per animal, compatibility/year tables, internal links, metadata, Article JSON-LD, FAQ JSON-LD, and responsive UI.

Purpose: Capture zodiac SEO traffic while keeping pages useful as tool-like entrances into auspicious-day and almanac flows.
Output: Zodiac page family rendered from typed static content and ready for sitemap inclusion.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seo-content-matrix/03-CONTEXT.md
@.planning/phases/03-seo-content-matrix/03-RESEARCH.md
@.planning/phases/03-seo-content-matrix/03-PATTERNS.md
@.planning/phases/03-seo-content-matrix/03-UI-SPEC.md
@.planning/phases/03-seo-content-matrix/03-01-PLAN.md

<interfaces>
- Plan 03-01 provides content registry, SEO helpers, and shared SEO components.
- Zodiac UI should use `zodiac-ring.png` or compatible existing assets from `/public/assets/almanac-icons`.
- Article body width must stay around `65ch`; desktop can use sticky right rail; mobile uses related chips.
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Article copy infringes or overfits a single source | high | Store short source note metadata per article, synthesize from multiple references, and avoid verbatim copied passages. |
| Article pages become thin cloned templates | medium | Tests require unique article slug, title, deck, body, FAQ, and related links. |
| Invalid animal/article slugs expose blank pages | medium | Static params from registry and `notFound()` on missing profile/article. |
| JSON-LD claims content not visible on page | medium | Article and FAQ JSON-LD source must come from the same registry content rendered visibly. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Zodiac content registry and editorial source notes</name>
  <files>
    src/lib/content/zodiac.ts,
    tests/seo/zodiac-content.test.ts
  </files>
  <read_first>
    src/lib/content/types.ts,
    src/lib/content/localize.ts,
    src/lib/content/registry.ts,
    .planning/phases/03-seo-content-matrix/03-CONTEXT.md,
    .planning/phases/03-seo-content-matrix/03-UI-SPEC.md
  </read_first>
  <action>
1. Expand `src/lib/content/zodiac.ts` with 12 zodiac profiles using stable slugs in lowercase pinyin or the existing planner-chosen slug convention.
2. Each profile must include animal name, years table seed data, personality summary, compatibility highlights, annual fortune summary, suitable/unsuitable actions, related jieri links, FAQ, and hub SEO fields.
3. Add 2-3 article entries per animal for personality, compatibility, and annual fortune topics. Each article must include source-note metadata that records at least two source categories or references consulted during writing.
4. Write natural, flowing Chinese editorial copy in Simplified canonical form and avoid phrases that read like generic AI summaries. Do not paste long verbatim source passages.
5. Add tests that assert 12 animals exist, each animal has 2-3 articles, total articles are between 24 and 36, and every article has FAQ and related links.
  </action>
  <verify>
    <automated>npx vitest run tests/seo/zodiac-content.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/content/zodiac.ts` exports `zodiacAnimals` or equivalent 12-item array
    - Every zodiac profile has at least one compatibility highlight and one related jieri link
    - Total zodiac articles count is at least 24 and at most 36
    - Every article includes source-note metadata with at least two entries
    - `tests/seo/zodiac-content.test.ts` exits 0
  </acceptance_criteria>
  <done>Zodiac content registry contains 12 hubs and 24-36 article entries with source notes, FAQs, and internal links.</done>
</task>

<task type="auto">
  <name>Task 2: Zodiac routes and metadata</name>
  <files>
    src/app/[locale]/zodiac/page.tsx,
    src/app/[locale]/zodiac/[animal]/page.tsx,
    src/app/[locale]/zodiac/[animal]/[slug]/page.tsx,
    tests/seo/zodiac-routes.test.ts
  </files>
  <read_first>
    src/app/[locale]/solar-terms/page.tsx,
    src/app/[locale]/jieri/page.tsx,
    src/lib/content/zodiac.ts,
    src/lib/seo.ts,
    src/components/seo/FaqBlock.tsx,
    src/components/seo/ArticleLayout.tsx,
    .planning/phases/03-seo-content-matrix/03-PATTERNS.md
  </read_first>
  <action>
1. Create `/[locale]/zodiac/page.tsx` as a 12-animal directory with concise intros and links to hubs.
2. Create `/[locale]/zodiac/[animal]/page.tsx` with `generateStaticParams` for 12 animals per locale, `generateMetadata`, BreadcrumbList JSON-LD, FAQPage JSON-LD, and WebPage JSON-LD.
3. Create `/[locale]/zodiac/[animal]/[slug]/page.tsx` with static params for every article, Article JSON-LD, FAQPage JSON-LD, and visible breadcrumbs.
4. Use typed registry lookup and `notFound()` for invalid animal or article slugs.
5. Ensure same-locale internal links point to related zodiac articles, auspicious day scenes, BaZi/naming tool entrances, and almanac pages where relevant.
  </action>
  <verify>
    <automated>npx vitest run tests/seo/zodiac-routes.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `/src/app/[locale]/zodiac/[animal]/page.tsx` exports `generateStaticParams`
    - `/src/app/[locale]/zodiac/[animal]/[slug]/page.tsx` exports `generateStaticParams`
    - Route tests assert 12 hub params are generated
    - Route tests assert article params count is between 24 and 36
    - Hub and article pages call `notFound()` when registry lookup fails
    - Article page renders `buildArticleJsonLd` or an equivalent Article JSON-LD helper
  </acceptance_criteria>
  <done>Zodiac index, hub, and article routes are static, metadata-rich, and registry-backed.</done>
</task>

<task type="auto">
  <name>Task 3: Zodiac hub and article UI components</name>
  <files>
    src/components/zodiac/ZodiacIndex.tsx,
    src/components/zodiac/ZodiacHub.tsx,
    src/components/zodiac/ZodiacYearTable.tsx,
    src/components/zodiac/ZodiacCompatibility.tsx
  </files>
  <read_first>
    src/components/seo/SeoPageShell.tsx,
    src/components/seo/SeoHero.tsx,
    src/components/seo/FaqBlock.tsx,
    src/components/seo/InternalLinkGrid.tsx,
    src/components/seo/ArticleLayout.tsx,
    src/components/ui/badge.tsx,
    src/components/ui/table.tsx,
    src/components/ui/scroll-area.tsx,
    src/styles/globals.css,
    .planning/phases/03-seo-content-matrix/03-UI-SPEC.md
  </read_first>
  <action>
1. Build `ZodiacIndex` for the directory page using dense icon/list rows rather than oversized marketing cards.
2. Build `ZodiacHub` with first-viewport animal H1, visible zodiac visual signal, year table/lookup, compatibility highlights, personality summary, fortune summary, suitable/unsuitable actions, and related auspicious day entrances.
3. Build `ZodiacYearTable` with compact desktop table and mobile horizontal scroll or accordion behavior.
4. Build `ZodiacCompatibility` using badges/chips and clear auspicious/caution visual distinctions.
5. Use actual assets from `/public/assets/almanac-icons` when available and lucide icons otherwise.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ZodiacHub` renders animal name as H1 and includes a visual asset or lucide zodiac-compatible icon near the first viewport
    - `ZodiacHub` renders compatibility, personality, fortune summary, suitable actions, unsuitable actions, and related jieri links
    - `ZodiacYearTable` uses horizontal overflow, compact table, or accordion behavior on mobile
    - Article route uses `ArticleLayout` and visible FAQ content
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Zodiac pages meet UI-SPEC first-viewport and responsive requirements.</done>
</task>

</tasks>

<verification>
Run after all tasks:
- `npm test -- tests/seo/zodiac-content.test.ts tests/seo/zodiac-routes.test.ts`
- `npx tsc --noEmit`
- `npm run build`
</verification>

<success_criteria>
- 12 zodiac hub pages render for both locales.
- 24-36 zodiac article pages render for both locales.
- Each hub and article has metadata, visible FAQ, internal links, and correct JSON-LD.
- Copy is natural, source-synthesized, and not duplicated from a single source.
</success_criteria>
