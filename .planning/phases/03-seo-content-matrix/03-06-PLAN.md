---
phase: 03-seo-content-matrix
plan: 06
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - src/app/[locale]/sitemap.ts
  - src/lib/content/registry.ts
  - src/lib/seo.ts
  - tests/seo/sitemap.test.ts
  - tests/seo/structured-data-coverage.test.ts
  - tests/seo/metadata-coverage.test.ts
  - tests/seo/phase3-smoke.test.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
  - SEO-06
  - SEO-07
  - SEO-08
  - SEO-09

must_haves:
  truths:
    - "D-21 sitemap is generated by hand with Next route APIs, typed content, locales, and year strategy"
    - "D-22 every indexable Phase 3 page has canonical URL, zh-Hans and zh-Hant hreflang, x-default, and Content-Language metadata"
    - "D-23 JSON-LD matches page type: WebApplication/WebPage for tools, Article for articles, FAQPage for FAQs, WebSite for site-level data, and BreadcrumbList for breadcrumbs"
    - "D-24 structured data helpers are reused from src/lib/seo.ts or adjacent centralized helpers"
    - "D-25 sitemap includes homepage, core almanac pages, calendar/solar terms, `/jieri` scenes for 2006-2046, zodiac hubs, zodiac articles, Feng Shui articles, and tool entrances; dynamic 0-5000 year pages are accessible but not all listed"
    - "D-11 final coverage verifies each indexable page family has unique main copy, FAQ, and internal links"
  artifacts:
    - path: "src/app/[locale]/sitemap.ts"
      provides: "Locale sitemap route"
      contains: "MetadataRoute.Sitemap"
    - path: "tests/seo/sitemap.test.ts"
      provides: "Sitemap route and alternate coverage tests"
      contains: "2006"
    - path: "tests/seo/structured-data-coverage.test.ts"
      provides: "JSON-LD coverage checks"
      contains: "FAQPage"
    - path: "tests/seo/metadata-coverage.test.ts"
      provides: "Canonical/hreflang/Content-Language coverage checks"
      contains: "x-default"
  key_links:
    - from: "src/app/[locale]/sitemap.ts"
      to: "src/lib/content/registry.ts"
      via: "sitemap candidate generation"
      pattern: "getSitemapCandidates"
    - from: "tests/seo/structured-data-coverage.test.ts"
      to: "src/lib/seo.ts"
      via: "shared JSON-LD helper assertions"
      pattern: "buildFaqJsonLd"
---

<objective>
Finish Phase 3 with sitemap generation, metadata/structured-data coverage tests, representative browser smoke checks, and build verification across all SEO matrix route families.

Purpose: Search engines can discover the Phase 3 matrix with correct alternates, canonical URLs, Content-Language, and page-type JSON-LD.
Output: Locale sitemap routes and coverage tests proving SEO-01 through SEO-09 are represented.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seo-content-matrix/03-CONTEXT.md
@.planning/phases/03-seo-content-matrix/03-RESEARCH.md
@.planning/phases/03-seo-content-matrix/03-PATTERNS.md
@.planning/phases/03-seo-content-matrix/03-UI-SPEC.md
@.planning/phases/03-seo-content-matrix/03-VALIDATION.md
@.planning/phases/03-seo-content-matrix/03-01-PLAN.md
@.planning/phases/03-seo-content-matrix/03-02-PLAN.md
@.planning/phases/03-seo-content-matrix/03-03-PLAN.md
@.planning/phases/03-seo-content-matrix/03-04-PLAN.md
@.planning/phases/03-seo-content-matrix/03-05-PLAN.md

<interfaces>
- Route families from Plans 03-02 through 03-05 are expected to exist before this plan runs.
- Next metadata route `app/[locale]/sitemap.ts` returns `MetadataRoute.Sitemap`.
- Registry and SEO helpers from Plan 03-01 are the single source for URLs, alternates, and structured data.
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Sitemap includes invalid or non-existent URLs | high | Sitemap tests import registry route candidates and verify representative routes exist in static params or route files. |
| Sitemap includes all legal years 0-5000, causing crawl bloat | high | Sitemap generator includes only indexed jieri years 2006-2046. |
| Metadata alternates disagree with sitemap alternates | medium | Coverage tests compare helper-generated languages against sitemap `alternates.languages`. |
| JSON-LD helper drift creates invalid page-type schema | medium | Structured-data tests assert required `@type`, URL, inLanguage, and visible FAQ/article fields. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Locale sitemap route from registry</name>
  <files>
    src/app/[locale]/sitemap.ts,
    src/lib/content/registry.ts,
    tests/seo/sitemap.test.ts
  </files>
  <read_first>
    src/lib/content/registry.ts,
    src/lib/almanac/year-support.ts,
    src/lib/seo.ts,
    src/i18n/routing.ts,
    src/i18n/locale-path.ts,
    .planning/phases/03-seo-content-matrix/03-CONTEXT.md
  </read_first>
  <action>
1. Create `src/app/[locale]/sitemap.ts` returning `MetadataRoute.Sitemap`.
2. Generate entries from the typed registry and indexed jieri years 2006-2046 only.
3. Include homepage, core almanac pages, calendar, solar terms, jieri scene-year pages, zodiac hubs, zodiac articles, Feng Shui articles, tools index, BaZi tool, and naming tool.
4. Add `alternates.languages` with `zh-Hans`, `zh-Hant`, and `x-default` URLs for each entry.
5. Add tests that assert 2006 and 2046 jieri URLs exist, 0 and 5000 jieri URLs do not exist in sitemap, both locales are represented in alternates, and tool/article URLs are present.
  </action>
  <verify>
    <automated>npx vitest run tests/seo/sitemap.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/[locale]/sitemap.ts` imports `MetadataRoute` from `next`
    - Sitemap includes `/zh-hant/jieri/{scene}/2006` and `/zh-hant/jieri/{scene}/2046` for at least one scene
    - Sitemap does not include `/jieri/{scene}/0`, `/jieri/{scene}/1`, or `/jieri/{scene}/5000`
    - Every sitemap entry has alternates for `zh-Hans`, `zh-Hant`, and `x-default`
    - `tests/seo/sitemap.test.ts` exits 0
  </acceptance_criteria>
  <done>Locale sitemap is generated by hand from the registry with correct indexed URLs and hreflang alternates.</done>
</task>

<task type="auto">
  <name>Task 2: Metadata and structured-data coverage tests</name>
  <files>
    tests/seo/structured-data-coverage.test.ts,
    tests/seo/metadata-coverage.test.ts,
    src/lib/seo.ts
  </files>
  <read_first>
    src/lib/seo.ts,
    src/lib/content/registry.ts,
    src/app/[locale]/jieri/[scene]/[year]/page.tsx,
    src/app/[locale]/zodiac/[animal]/page.tsx,
    src/app/[locale]/zodiac/[animal]/[slug]/page.tsx,
    src/app/[locale]/tools/bazi/page.tsx,
    src/app/[locale]/tools/naming/page.tsx,
    src/app/[locale]/feng-shui/[category]/[slug]/page.tsx
  </read_first>
  <action>
1. Add `tests/seo/metadata-coverage.test.ts` to assert representative jieri, zodiac hub, zodiac article, BaZi tool, naming tool, and Feng Shui article metadata includes canonical path, `zh-Hans`, `zh-Hant`, `x-default`, keywords, and Content-Language.
2. Add `tests/seo/structured-data-coverage.test.ts` to assert representative pages produce page-type JSON-LD matching their visible content: WebPage/WebApplication for tools, Article for articles, FAQPage for FAQ, and BreadcrumbList for breadcrumbs.
3. If route modules cannot be imported directly due Next runtime constraints, test the registry and SEO helper functions that route modules consume, and add source assertions that each route imports the relevant helper.
  </action>
  <verify>
    <automated>npx vitest run tests/seo/metadata-coverage.test.ts tests/seo/structured-data-coverage.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Metadata coverage tests mention representative route families: jieri, zodiac, zodiac article, bazi, naming, and feng-shui
    - Metadata coverage tests assert `zh-Hans`, `zh-Hant`, `x-default`, and `content-language`
    - Structured-data tests assert `FAQPage`, `BreadcrumbList`, `Article`, and `WebApplication` or `WebPage`
    - Route source assertions confirm route files import centralized helpers instead of assembling all JSON-LD inline
  </acceptance_criteria>
  <done>Metadata and structured-data coverage tests protect all Phase 3 page families.</done>
</task>

<task type="auto">
  <name>Task 3: Final build and browser smoke verification</name>
  <files>
    tests/seo/phase3-smoke.test.ts
  </files>
  <read_first>
    package.json,
    .planning/phases/03-seo-content-matrix/03-VALIDATION.md,
    src/app/[locale]/jieri/[scene]/[year]/page.tsx,
    src/app/[locale]/zodiac/[animal]/page.tsx,
    src/app/[locale]/zodiac/[animal]/[slug]/page.tsx,
    src/app/[locale]/tools/bazi/page.tsx,
    src/app/[locale]/tools/naming/page.tsx,
    src/app/[locale]/feng-shui/[category]/[slug]/page.tsx,
    src/app/[locale]/sitemap.ts
  </read_first>
  <action>
1. Add a lightweight smoke test or documented smoke script under `tests/seo/phase3-smoke.test.ts` that enumerates representative URLs for `/jieri/{scene}/2026`, `/zodiac/{animal}`, a zodiac article, a Feng Shui article, `/tools/bazi`, `/tools/naming`, and `/sitemap.xml`.
2. Run full automated checks: `npm run lint`, `npm test`, `npx tsc --noEmit`, and `npm run build`.
3. Run browser smoke manually or with Playwright after starting the app if the project has a Playwright setup. Check desktop and 375px/320px mobile for representative pages, no horizontal body overflow, visible H1, visible FAQ, form labels, and at least one JSON-LD script.
4. Record any manual smoke findings in the plan summary; do not mark verification green if build fails or representative pages 404.
  </action>
  <verify>
    <automated>npm run lint && npm test && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npm test` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - Representative route list includes jieri, zodiac hub, zodiac article, Feng Shui article, BaZi tool, naming tool, and sitemap
    - Manual/browser smoke notes confirm no horizontal overflow at 375px and 320px or list exact failing URLs for follow-up
  </acceptance_criteria>
  <done>Phase 3 SEO matrix passes full automated checks and representative route smoke verification.</done>
</task>

</tasks>

<verification>
Run after all tasks:
- `npm run lint`
- `npm test`
- `npx tsc --noEmit`
- `npm run build`
</verification>

<success_criteria>
- XML sitemap routes are generated from the typed registry with hreflang alternates.
- Metadata and JSON-LD coverage exists for all Phase 3 page families.
- Sitemap includes the chosen indexable matrix and excludes full 0-5000 dynamic expansion.
- Full lint, test, typecheck, and build verification pass.
</success_criteria>
