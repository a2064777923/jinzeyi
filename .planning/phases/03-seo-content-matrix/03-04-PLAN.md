---
phase: 03-seo-content-matrix
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/almanac/bazi.ts
  - src/lib/tools/china-cities.ts
  - src/lib/tools/naming.ts
  - src/lib/content/tools.ts
  - src/components/tools/BaziForm.tsx
  - src/components/tools/BaziResult.tsx
  - src/components/tools/NamingForm.tsx
  - src/components/tools/NamingResult.tsx
  - src/app/[locale]/tools/page.tsx
  - src/app/[locale]/tools/bazi/page.tsx
  - src/app/[locale]/tools/naming/page.tsx
  - tests/almanac/bazi.test.ts
  - tests/tools/naming.test.ts
autonomous: true
requirements:
  - SEO-04
  - SEO-06
  - SEO-08
  - SEO-09

must_haves:
  truths:
    - "D-12 BaZi MVP outputs four pillars, heavenly stems/earthly branches, five-element distribution, and non-personalized cultural explanation only"
    - "D-13 BaZi input supports birth date, precise birth time, birth place, and gender, with consumer metaphysics UX inspired by Cece/测测 but bounded to Phase 3"
    - "D-14 birthplace supports China city/province true solar time correction first, without global timezone or international true solar time"
    - "D-15 Naming MVP includes character five-element attributes, basic auspicious/inauspicious explanation, initial score, and suggested characters, without BaZi useful-god integration"
    - "D-09 tool SEO uses locale-specific title/description/H1 plus Simplified canonical body copy converted by OpenCC"
    - "D-11 BaZi and naming pages render unique main copy, visible FAQ, and internal links"
  artifacts:
    - path: "src/lib/almanac/bazi.ts"
      provides: "Four-pillar and five-element utilities"
      contains: "calculateBazi"
    - path: "src/lib/tools/china-cities.ts"
      provides: "China city/province longitude dataset for true solar time"
      contains: "CHINA_CITIES"
    - path: "src/lib/tools/naming.ts"
      provides: "Name five-element scoring and suggestions"
      contains: "analyzeName"
    - path: "src/app/[locale]/tools/bazi/page.tsx"
      provides: "BaZi tool SEO page"
      contains: "BaziForm"
    - path: "src/app/[locale]/tools/naming/page.tsx"
      provides: "Naming tool SEO page"
      contains: "NamingForm"
  key_links:
    - from: "src/components/tools/BaziForm.tsx"
      to: "src/lib/almanac/bazi.ts"
      via: "client result calculation or server-safe utility import"
      pattern: "calculateBazi"
    - from: "src/components/tools/NamingForm.tsx"
      to: "src/lib/tools/naming.ts"
      via: "name analysis utility"
      pattern: "analyzeName"
---

<objective>
Build the Phase 3 tools slice: BaZi chart page, naming five-element page, and tools index with server-rendered SEO content plus isolated client form/result widgets.

Purpose: Users can enter birth data for a basic BaZi chart and enter a name for five-element/name auspiciousness analysis, while search engines still see complete tool content, FAQs, and structured data.
Output: `/tools`, `/tools/bazi`, and `/tools/naming` route family with tested calculation utilities.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seo-content-matrix/03-CONTEXT.md
@.planning/phases/03-seo-content-matrix/03-RESEARCH.md
@.planning/phases/03-seo-content-matrix/03-PATTERNS.md
@.planning/phases/03-seo-content-matrix/03-UI-SPEC.md
@.planning/phases/03-seo-content-matrix/03-VALIDATION.md
@.planning/phases/03-seo-content-matrix/03-01-PLAN.md

<interfaces>
- Use tyme4ts `SolarTime.fromYmdHms(...).getLunarHour().getEightChar()` for verified BaZi calculations.
- Use Zod for form validation.
- Keep route pages as Server Components; only form/result widgets are client components.
- Existing form pattern: `src/components/almanac/DateSearchForm.tsx`.
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| User input causes invalid date/time/city computation | high | Zod schemas validate date, time, city, gender, surname, and given name before calculation. |
| Tool output makes deterministic destiny or medical/financial claims | medium | Copy is bounded to cultural explanation and non-personalized interpretation. |
| Client-only rendering hides SEO content | medium | Tool route pages remain Server Components with visible body copy, FAQ, and JSON-LD. |
| Large city dataset bloats client bundle | low | Keep city dataset compact for MVP and import only in the widget that needs it. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: BaZi utility, true solar time dataset, and tests</name>
  <files>
    src/lib/almanac/bazi.ts,
    src/lib/tools/china-cities.ts,
    tests/almanac/bazi.test.ts
  </files>
  <read_first>
    src/lib/almanac/service.ts,
    src/lib/almanac/year-support.ts,
    tests/almanac/tyme4ts.test.ts,
    .planning/phases/03-seo-content-matrix/03-RESEARCH.md,
    .planning/phases/03-seo-content-matrix/03-CONTEXT.md
  </read_first>
  <action>
1. Create `src/lib/tools/china-cities.ts` with an MVP list of major Chinese cities/province capitals. Each city entry must include stable id, display name, province, longitude, and UTC+8 timezone assumption.
2. Create `src/lib/almanac/bazi.ts` with Zod-compatible input types for birth date, precise time, birthplace city id, and gender.
3. Implement true solar time correction using longitude offset from the UTC+8 standard meridian 120E: correction minutes equals `(longitude - 120) * 4`. Apply this before calling tyme4ts.
4. Implement `calculateBazi(input)` returning four pillars, heavenly stems/earthly branches, five-element distribution, true solar time adjustment summary, gender, city, and bounded cultural explanation.
5. Add tests for fixed input `2026-05-17 11:30` that assert year pillar `丙午`, month pillar `癸巳`, day pillar `辛卯`, and hour pillar `甲午` when using the reference city/time case from RESEARCH.md.
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/bazi.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/tools/china-cities.ts` exports `CHINA_CITIES` with at least 20 China city/province entries
    - `src/lib/almanac/bazi.ts` exports `calculateBazi`
    - `calculateBazi` return value includes `pillars.year`, `pillars.month`, `pillars.day`, `pillars.hour`, and `elements`
    - Tests assert the RESEARCH.md reference pillars `丙午`, `癸巳`, `辛卯`, and `甲午`
    - Tests assert true solar time correction changes by 4 minutes per longitude degree from 120E
  </acceptance_criteria>
  <done>BaZi calculation utility supports four pillars, five-element distribution, China city true solar time, birthplace, and gender.</done>
</task>

<task type="auto">
  <name>Task 2: Naming utility and tests</name>
  <files>
    src/lib/tools/naming.ts,
    tests/tools/naming.test.ts
  </files>
  <read_first>
    src/lib/content/tools.ts,
    .planning/phases/03-seo-content-matrix/03-CONTEXT.md,
    .planning/phases/03-seo-content-matrix/03-RESEARCH.md
  </read_first>
  <action>
1. Create `src/lib/tools/naming.ts` with a compact character five-element dictionary for common naming characters and fallback handling for unknown characters.
2. Implement `analyzeName({ surname, givenName })` returning per-character element, known/unknown status, score 0-100, auspicious/inauspicious explanation, and suggested replacement/name characters.
3. Do not read BaZi useful-god data or combine with BaZi output in Phase 3.
4. Use Zod or equivalent validation for Chinese surname/given name length and reject empty, numeric, or symbol-only names.
5. Add tests for known characters, unknown characters, empty input rejection, score range, and suggestion list presence.
  </action>
  <verify>
    <automated>npx vitest run tests/tools/naming.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/tools/naming.ts` exports `analyzeName`
    - `analyzeName` returns score between 0 and 100
    - Result includes per-character element entries for surname and given name
    - Result includes suggested characters or names as selectable string entries
    - Tests assert no BaZi useful-god input is required
  </acceptance_criteria>
  <done>Naming utility provides five-element attributes, score, explanation, and suggestions without BaZi useful-god coupling.</done>
</task>

<task type="auto">
  <name>Task 3: Tool pages, client widgets, metadata, and JSON-LD</name>
  <files>
    src/components/tools/BaziForm.tsx,
    src/components/tools/BaziResult.tsx,
    src/components/tools/NamingForm.tsx,
    src/components/tools/NamingResult.tsx,
    src/app/[locale]/tools/page.tsx,
    src/app/[locale]/tools/bazi/page.tsx,
    src/app/[locale]/tools/naming/page.tsx,
    src/lib/content/tools.ts
  </files>
  <read_first>
    src/components/almanac/DateSearchForm.tsx,
    src/components/seo/SeoPageShell.tsx,
    src/components/seo/SeoHero.tsx,
    src/components/seo/FaqBlock.tsx,
    src/components/seo/InternalLinkGrid.tsx,
    src/components/ui/button.tsx,
    src/components/ui/card.tsx,
    src/components/ui/badge.tsx,
    src/components/ui/tabs.tsx,
    src/lib/seo.ts,
    src/lib/content/tools.ts,
    .planning/phases/03-seo-content-matrix/03-UI-SPEC.md
  </read_first>
  <action>
1. Create `/[locale]/tools/page.tsx` as a tool directory linking to BaZi and naming tools.
2. Create `/[locale]/tools/bazi/page.tsx` and `/[locale]/tools/naming/page.tsx` as Server Components with `generateMetadata`, visible SEO body copy, FAQ, internal links, breadcrumbs, and WebApplication plus FAQ JSON-LD.
3. Create `BaziForm` and `BaziResult` client components. The form must include visible labels for birth date, precise time, birth place, and gender; inline validation; and result area below the form.
4. Create `NamingForm` and `NamingResult` client components. The form must include visible labels for surname and given name; inline validation; and selectable suggestion chips or compact rows.
5. Results must have stable dimensions: four pillars as four columns on desktop and 2x2 grid on mobile; five-element bars/chips must not cause horizontal overflow at 320px.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/tools/bazi` route renders H1 `八字排盘` or locale equivalent in initial HTML
    - `BaziForm` contains fields for birth date, precise birth time, birth place, and gender with visible labels
    - BaZi result renders four stable pillar cells and five-element distribution
    - `/tools/naming` route renders H1 `姓名五行查询` or locale equivalent in initial HTML
    - `NamingForm` contains surname and given name fields with visible labels
    - Naming result renders per-character element, score, explanation, and suggestions
    - Tool pages render WebApplication or WebPage JSON-LD and FAQPage JSON-LD
  </acceptance_criteria>
  <done>BaZi and naming tool pages are SEO-rendered with accessible client form/result widgets.</done>
</task>

</tasks>

<verification>
Run after all tasks:
- `npm test -- tests/almanac/bazi.test.ts tests/tools/naming.test.ts`
- `npx tsc --noEmit`
- `npm run build`
</verification>

<success_criteria>
- `/tools/bazi` accepts birth date, time, birthplace, and gender and displays four pillars plus five-element distribution.
- `/tools/naming` analyzes surname/given name and displays element attributes, score, explanation, and suggested characters.
- Tool pages remain server-rendered for SEO and include metadata, visible FAQ, internal links, and JSON-LD.
</success_criteria>
