---
phase: 04-ai-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/almanac/bazi.ts
  - tests/almanac/bazi.test.ts
autonomous: true
requirements:
  - META-01
  - META-04

must_haves:
  truths:
    - "D-01 AI assistant/provider integration is postponed; do not add AI routes, provider registry, Vercel AI SDK, or model failover in this plan"
    - "D-02 Phase 4 is implemented as Metaphysics Depth & Product Experience Upgrade despite the legacy directory name"
    - "D-04 BaZi data model is professional-chart-first while preserving a beginner-readable explanation layer"
    - "D-05 First BaZi professional chart includes four pillars, day master, heavenly stems, earthly branches, ten gods, hidden stems, na-yin, and five-element distribution/strength signals"
    - "D-06 Shen-sha lists and luck cycles are deferred from this first slice"
    - "D-07 BaZi terminology fields must support richer explanations downstream"
    - "D-08 Professional chart is the source of truth; do not generate hard fate claims"
    - "D-21 Output is cultural research and folklore reference, not deterministic advice"
    - "D-22 Use tendency/reference/use with caution language"
    - "D-23 Methodology is visible through explicit chart fields and reason strings"
  artifacts:
    - path: "src/lib/almanac/bazi.ts"
      provides: "Professional BaZi calculation core"
      contains: "calculateBazi"
    - path: "tests/almanac/bazi.test.ts"
      provides: "Regression tests for true solar time and professional chart fields"
      contains: "2005-12-23"
  key_links:
    - from: "src/components/tools/BaziForm.tsx"
      to: "src/lib/almanac/bazi.ts"
      via: "calculateBazi"
      pattern: "BaziResult"
---

<objective>
Upgrade the BaZi calculation core from a basic four-pillar/five-element result into a professional chart model.

Purpose: Later UI and scoring plans can consume deterministic BaZi data without adding AI or external APIs.
Output: `calculateBazi` returns backward-compatible existing fields plus professional chart fields for day master, ten gods, hidden stems, na-yin, terrain, and strength signals.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-integration/04-CONTEXT.md
@.planning/phases/04-ai-integration/04-RESEARCH.md
@.planning/phases/04-ai-integration/04-PATTERNS.md
@.planning/phases/04-ai-integration/04-UI-SPEC.md
@.planning/phases/04-ai-integration/04-VALIDATION.md

<interfaces>
- Continue using `SolarTime.fromYmdHms(...).getLunarHour().getEightChar()`.
- Preserve existing `BaziResult` fields: `input`, `city`, `genderLabel`, `trueSolarTime`, `pillars`, `elements`, `explanation`.
- Add professional fields without breaking existing tests and UI imports.
- Do not implement `ChildLimit`, da-yun, liu-nian, liu-yue, liu-ri, or shen-sha in this plan.
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Invalid date/time/city data produces misleading chart | high | Keep existing Zod/date/city validation and extend tests for invalid input. |
| Backward-incompatible result shape breaks Phase 3 tool pages | high | Preserve current top-level fields and update tests to assert backward compatibility. |
| Ten-god or hidden-stem relation is calculated against the wrong stem | high | Use day master stem as relation origin and add fixed fixture assertions from RESEARCH.md. |
| Chart explanation makes deterministic fate claims | medium | Keep explanation bounded to cultural-reference wording and source fields. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend BaZi professional types and helpers</name>
  <files>
    src/lib/almanac/bazi.ts
  </files>
  <read_first>
    src/lib/almanac/bazi.ts,
    tests/almanac/bazi.test.ts,
    node_modules/tyme4ts/dist/lib/index.d.ts,
    .planning/phases/04-ai-integration/04-RESEARCH.md,
    .planning/phases/04-ai-integration/04-CONTEXT.md
  </read_first>
  <action>
1. In `src/lib/almanac/bazi.ts`, add exported types for `BaziHiddenStem`, `BaziProfessionalPillar`, `BaziDayMaster`, `BaziElementStrength`, and `BaziProfessionalChart`.
2. Extend `BaziResult` with a new `professional: BaziProfessionalChart` field while preserving existing top-level fields.
3. For every pillar, use `SixtyCycle#getHeavenStem`, `getEarthBranch`, and `getSound` to populate `heavenlyStem`, `earthlyBranch`, `naYin`, and existing element fields.
4. Use the day pillar heaven stem as `dayMaster`; calculate visible stem ten-gods with `dayMasterStem.getTenStar(pillarStem)`.
5. Use each branch's `getHideHeavenStems()` and calculate each hidden stem's ten-god with `dayMasterStem.getTenStar(hiddenStem.getHeavenStem())`.
6. Add day-master terrain/twelve-growth signal per branch with `dayMasterStem.getTerrain(branch)`.
7. Do not call `ChildLimit` or add luck-cycle output in this plan.
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/bazi.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/almanac/bazi.ts` exports `BaziProfessionalChart`
    - `BaziResult` includes `professional`
    - `professional.dayMaster.heavenlyStem` is derived from the day pillar stem
    - Every `professional.pillars.{year,month,day,hour}` entry includes `tenGod`, `hiddenStems`, `naYin`, and `terrain`
    - Existing `result.pillars.year.value` and `result.elements` fields still exist
  </acceptance_criteria>
  <done>BaZi result model can represent professional chart details while staying backward compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Add professional element strength signals</name>
  <files>
    src/lib/almanac/bazi.ts
  </files>
  <read_first>
    src/lib/almanac/bazi.ts,
    src/lib/almanac/types.ts,
    .planning/phases/04-ai-integration/04-RESEARCH.md
  </read_first>
  <action>
1. Keep the existing visible `elements` count based on four visible stems and four visible branches.
2. Add `professional.elementStrength` with `visibleCounts`, `hiddenStemWeightedCounts`, `combinedScores`, `strongest`, `weakest`, and `summary`.
3. Use deterministic hidden-stem weights by hidden-stem type: main `3`, middle `2`, residual `1`; if tyme4ts returns numeric type values, map `2` to main, `1` to middle, and `0` to residual as observed in RESEARCH.md.
4. Summary copy must use "较明显", "偏少", "参考", or equivalent cautious language; it must not say a person will definitely be rich, sick, married, promoted, or unlucky.
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/bazi.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `professional.elementStrength.visibleCounts` contains keys `木`, `火`, `土`, `金`, `水`
    - `professional.elementStrength.hiddenStemWeightedCounts` contains keys `木`, `火`, `土`, `金`, `水`
    - `professional.elementStrength.combinedScores` contains keys `木`, `火`, `土`, `金`, `水`
    - `professional.elementStrength.summary` contains `参考` or `參考`
    - No new explanation string contains `必定`, `一定`, `保证`, or `保證`
  </acceptance_criteria>
  <done>Five-element output distinguishes visible counts from hidden-stem weighted strength signals.</done>
</task>

<task type="auto">
  <name>Task 3: Regression tests for professional chart fields</name>
  <files>
    tests/almanac/bazi.test.ts
  </files>
  <read_first>
    tests/almanac/bazi.test.ts,
    src/lib/almanac/bazi.ts,
    .planning/phases/04-ai-integration/04-RESEARCH.md
  </read_first>
  <action>
1. Add a fixture test for `2005-12-23 08:37` in `hangzhou` or an explicitly selected existing city.
2. Assert the professional chart includes day master `辛`, day pillar `辛巳`, and year/month/hour pillar strings matching the tyme4ts output in 04-RESEARCH.md after true solar time adjustment is considered.
3. Assert at least one visible ten-god value is `偏财`, one hidden stem under day branch includes `丙` with `正官`, and one pillar na-yin value is present.
4. Extend existing tests to assert old fields still exist.
5. Add a safety-copy assertion that `result.explanation` and `professional.elementStrength.summary` do not contain deterministic claim terms `必定`, `一定`, `保证`, or `保證`.
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/bazi.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains fixture date `2005-12-23`
    - Test asserts `professional.dayMaster.heavenlyStem`
    - Test asserts `hiddenStems`
    - Test asserts `naYin`
    - `npx vitest run tests/almanac/bazi.test.ts --reporter=verbose` exits 0
  </acceptance_criteria>
  <done>Professional BaZi calculation is protected by deterministic regression tests.</done>
</task>

</tasks>

<verification>
Run after all tasks:
- `npx vitest run tests/almanac/bazi.test.ts --reporter=verbose`
- `npx tsc --noEmit`
</verification>

<success_criteria>
- `calculateBazi` remains backward compatible with Phase 3 UI/tests.
- Professional chart fields exist for day master, ten gods, hidden stems, na-yin, terrain, and element strength.
- BaZi output is deterministic, tested, and free of AI/provider integration.
- Explanation copy stays in cultural-reference language.
</success_criteria>
