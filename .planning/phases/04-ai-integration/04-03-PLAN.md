---
phase: 04-ai-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/components/tools/BaziResult.tsx
  - src/components/tools/BaziForm.tsx
  - src/components/tools/BaziSummary.tsx
  - src/components/tools/BaziProfessionalChart.tsx
  - src/components/tools/BaziElementStrengthPanel.tsx
  - src/app/[locale]/tools/bazi/page.tsx
  - src/lib/content/tools.ts
  - tests/tools/bazi-ui-source.test.ts
autonomous: true
requirements:
  - META-01
  - META-04

must_haves:
  truths:
    - "D-04 BaZi page shows plain-language summary before the professional chart"
    - "D-05 UI exposes four-pillar breakdown, day master, heavenly stems, earthly branches, ten gods, hidden stems, na-yin, and five-element distribution/strength signals"
    - "D-06 Shen-sha and luck cycles remain deferred and should not appear as shipped controls"
    - "D-07 Terminology explanations include short definition, chart-specific hint, source notes where available, and plain-language example through knowledge hints/pages"
    - "D-08 UI avoids hard fate claims and treats the professional chart as evidence for explanation"
    - "D-19 Tool pages integrate knowledge through tooltip/popover term hints"
    - "D-21 Keep cultural research / folklore reference framing"
    - "D-22 Prefer tendency/reference/worth noting/more suitable/use with caution language"
    - "D-23 Make methodology visible enough that users understand why chart insights are shown"
  artifacts:
    - path: "src/components/tools/BaziResult.tsx"
      provides: "BaZi result orchestrator"
      contains: "BaziResult"
    - path: "src/components/tools/BaziProfessionalChart.tsx"
      provides: "Professional four-pillar chart UI"
      contains: "BaziProfessionalChart"
    - path: "src/components/tools/BaziElementStrengthPanel.tsx"
      provides: "Visible and hidden-stem five-element strength UI"
      contains: "BaziElementStrengthPanel"
  key_links:
    - from: "src/components/tools/BaziResult.tsx"
      to: "src/lib/almanac/bazi.ts"
      via: "professional result fields"
      pattern: "professional"
    - from: "src/components/tools/BaziProfessionalChart.tsx"
      to: "src/lib/content/metaphysics.ts"
      via: "TermHint entries"
      pattern: "ten-gods"
---

<objective>
Upgrade the BaZi tool UI into a summary-first professional chart experience.

Purpose: Users can understand their chart at a glance, then inspect professional details with term hints and knowledge links.
Output: `/tools/bazi` renders a plain-language summary, professional chart, element strength panel, and safe explanatory copy.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-integration/04-CONTEXT.md
@.planning/phases/04-ai-integration/04-RESEARCH.md
@.planning/phases/04-ai-integration/04-PATTERNS.md
@.planning/phases/04-ai-integration/04-UI-SPEC.md
@.planning/phases/04-ai-integration/04-VALIDATION.md
@.planning/phases/04-ai-integration/04-01-PLAN.md
@.planning/phases/04-ai-integration/04-02-PLAN.md

<interfaces>
- Requires Plan 04-01 `BaziResult.professional`.
- Requires Plan 04-02 rich `TermHint` and metaphysics entries.
- Keep `/tools/bazi` as a Server Component page with client form/result island.
- Do not add AI-generated interpretation.
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Professional chart overwhelms beginners | medium | Summary panel appears before detailed chart and uses plain-language bounded copy. |
| Mobile layout overflows due dense chart labels | high | Use stable grids, wrapping labels, and source-level/mobile manual checks from UI-SPEC. |
| Term hints become long story blocks inside the tool | medium | Hints stay compact and link to `/knowledge/[slug]` for long stories. |
| UI implies deterministic life outcomes | high | Source tests check banned terms and copy uses cultural-reference language. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Summary-first BaZi result composition</name>
  <files>
    src/components/tools/BaziResult.tsx,
    src/components/tools/BaziSummary.tsx,
    tests/tools/bazi-ui-source.test.ts
  </files>
  <read_first>
    src/components/tools/BaziResult.tsx,
    src/components/tools/BaziForm.tsx,
    src/lib/almanac/bazi.ts,
    src/components/ui/badge.tsx,
    src/components/ui/card.tsx,
    .planning/phases/04-ai-integration/04-UI-SPEC.md
  </read_first>
  <action>
1. Create `BaziSummary.tsx` that accepts the `BaziResultData` object and renders a compact summary before any professional chart details.
2. Summary must include city, gender label, true solar time adjustment, day master, strongest/weakest five-element signal, and one bounded interpretation sentence.
3. Update `BaziResult.tsx` to render `BaziSummary` first, then professional chart and element panels.
4. Keep the old result fallback behavior working if `professional` is absent during development, but final code should consume `result.professional`.
5. Add `tests/tools/bazi-ui-source.test.ts` source assertions for `BaziSummary`, `dayMaster`, `professional`, and banned terms `必定`, `一定`, `保证`, `保證`.
  </action>
  <verify>
    <automated>npx vitest run tests/tools/bazi-ui-source.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tools/BaziSummary.tsx` exports `BaziSummary`
    - `BaziResult.tsx` renders `BaziSummary` before `BaziProfessionalChart`
    - Source test asserts `dayMaster`
    - Source test asserts no banned deterministic terms in BaZi component copy
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>BaZi result begins with a beginner-readable summary sourced from professional chart data.</done>
</task>

<task type="auto">
  <name>Task 2: Professional pillar chart with term hints</name>
  <files>
    src/components/tools/BaziProfessionalChart.tsx,
    src/components/tools/BaziResult.tsx,
    tests/tools/bazi-ui-source.test.ts
  </files>
  <read_first>
    src/components/tools/BaziResult.tsx,
    src/components/knowledge/TermHint.tsx,
    src/lib/content/metaphysics.ts,
    src/lib/content/glossary.ts,
    src/lib/almanac/bazi.ts,
    src/components/ui/badge.tsx,
    src/components/ui/tabs.tsx,
    .planning/phases/04-ai-integration/04-UI-SPEC.md
  </read_first>
  <action>
1. Create `BaziProfessionalChart.tsx` that renders four stable pillar cells in order: year, month, day, hour.
2. Each pillar cell must show pillar value, heavenly stem, earthly branch, visible ten god, hidden stems with ten-god labels, na-yin, and terrain.
3. Use `TermHint` for at least `day-master`, `ten-gods`, `hidden-stems`, and `na-yin`.
4. Keep chart cells responsive: two columns on small screens, four columns on large screens, no horizontal scroll at 320px.
5. Update source tests to assert the component source contains `hiddenStems`, `naYin`, `tenGod`, and `TermHint`.
  </action>
  <verify>
    <automated>npx vitest run tests/tools/bazi-ui-source.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `BaziProfessionalChart.tsx` exports `BaziProfessionalChart`
    - Component source contains `hiddenStems`
    - Component source contains `naYin`
    - Component source contains `tenGod`
    - Component source imports or renders `TermHint`
  </acceptance_criteria>
  <done>Professional BaZi details are visible, structured, responsive, and linked to term explanations.</done>
</task>

<task type="auto">
  <name>Task 3: Element strength panel and page copy</name>
  <files>
    src/components/tools/BaziElementStrengthPanel.tsx,
    src/components/tools/BaziResult.tsx,
    src/app/[locale]/tools/bazi/page.tsx,
    src/lib/content/tools.ts,
    tests/tools/bazi-ui-source.test.ts
  </files>
  <read_first>
    src/components/tools/BaziResult.tsx,
    src/lib/content/tools.ts,
    src/app/[locale]/tools/bazi/page.tsx,
    src/components/knowledge/GlossaryPanel.tsx,
    src/components/knowledge/TermHint.tsx,
    src/lib/content/metaphysics.ts,
    .planning/phases/04-ai-integration/04-UI-SPEC.md
  </read_first>
  <action>
1. Create `BaziElementStrengthPanel.tsx` that displays visible counts, hidden-stem weighted counts, combined scores, strongest element, weakest element, and cautious summary.
2. Use bars or compact rows with stable dimensions; labels must wrap rather than overflow.
3. Update `/tools/bazi` surrounding content and FAQ in `src/lib/content/tools.ts` to mention professional chart fields: day master, ten gods, hidden stems, na-yin, and five-element strength.
4. Add or update page-level related links to relevant knowledge entries such as `/knowledge/day-master`, `/knowledge/ten-gods`, and `/knowledge/five-elements`.
5. Do not add shen-sha or luck-cycle controls in this plan.
  </action>
  <verify>
    <automated>npx vitest run tests/tools/bazi-ui-source.test.ts --reporter=verbose && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `BaziElementStrengthPanel.tsx` exports `BaziElementStrengthPanel`
    - Source contains `visibleCounts`
    - Source contains `hiddenStemWeightedCounts`
    - `/tools/bazi` content references `日主`
    - No BaZi page source contains `大运`, `流年`, `神煞` as a shipped control label
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>BaZi page shows professional five-element strength signals and updated safe explanatory content.</done>
</task>

</tasks>

<verification>
Run after all tasks:
- `npx vitest run tests/almanac/bazi.test.ts tests/tools/bazi-ui-source.test.ts --reporter=verbose`
- `npx tsc --noEmit`
- `npm run build`
</verification>

<success_criteria>
- `/tools/bazi` renders a plain-language summary before professional details.
- Professional chart UI exposes day master, ten gods, hidden stems, na-yin, terrain, and element strength.
- Term hints link BaZi labels to knowledge entries.
- The UI remains responsive and avoids deterministic fate claims.
</success_criteria>
