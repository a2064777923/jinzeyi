---
phase: 04-ai-integration
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/lib/almanac/types.ts
  - src/lib/almanac/auspicious.ts
  - src/lib/almanac/auspicious-scoring.ts
  - src/lib/content/jieri-scenes.ts
  - src/lib/content/tools.ts
  - src/components/jieri/AuspiciousRecommendationForm.tsx
  - src/components/jieri/AuspiciousRecommendationResult.tsx
  - src/components/jieri/ScoreBreakdown.tsx
  - src/components/jieri/JieriScenePage.tsx
  - src/app/[locale]/tools/jieri-recommend/page.tsx
  - src/lib/content/registry.ts
  - src/app/sitemap.ts
  - src/app/[locale]/sitemap.ts
  - tests/almanac/auspicious-scoring.test.ts
  - tests/seo/jieri-recommend-route.test.ts
autonomous: true
requirements:
  - META-02
  - META-04

must_haves:
  truths:
    - "D-09 Auspicious-date upgrade is an interactive recommendation flow, not only an enhanced SEO list"
    - "D-10 Scoring combines almanac data, scene rules, and BaZi/five-element signals"
    - "D-11 Input requirements depend on scene: marriage/matching two people, moving optional household members, business/signing responsible person/legal representative"
    - "D-12 Results show overall score/grade plus almanac fit, scene fit, zodiac/chong-sha, BaZi/five-element fit, and usable lucky hours"
    - "D-13 Every score, bonus, and penalty is explainable"
    - "D-14 Core flow is choose scene, enter people, choose date range, receive top recommended dates with score, reasons, cautions, and usable lucky hours"
    - "D-15 Existing `/jieri/{scene}/{year}` pages can link into or reuse the interactive flow, but should remain SEO lists"
    - "D-21 Keep cultural research / folklore reference framing"
    - "D-22 Prefer tendency/reference/worth noting/more suitable/use with caution language"
    - "D-23 Explain methodology visibly enough that users understand why a date or chart insight is shown"
  artifacts:
    - path: "src/lib/almanac/auspicious-scoring.ts"
      provides: "Personalized auspicious-date scoring service"
      contains: "scoreAuspiciousDateRange"
    - path: "src/components/jieri/AuspiciousRecommendationForm.tsx"
      provides: "Interactive recommendation flow"
      contains: "AuspiciousRecommendationForm"
    - path: "src/app/[locale]/tools/jieri-recommend/page.tsx"
      provides: "Locale-aware recommendation tool route"
      contains: "generateMetadata"
  key_links:
    - from: "src/lib/almanac/auspicious-scoring.ts"
      to: "src/lib/almanac/bazi.ts"
      via: "calculateBazi for people inputs"
      pattern: "calculateBazi"
    - from: "src/components/jieri/JieriScenePage.tsx"
      to: "src/app/[locale]/tools/jieri-recommend/page.tsx"
      via: "personalized recommendation CTA"
      pattern: "jieri-recommend"
---

<objective>
Build an interactive personalized auspicious-date recommendation flow.

Purpose: Users can move beyond annual lists by choosing a scene, entering relevant people, selecting a date range, and receiving ranked explainable date recommendations.
Output: deterministic scoring service, scene role configs, `/tools/jieri-recommend` route, form/result UI, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-integration/04-CONTEXT.md
@.planning/phases/04-ai-integration/04-RESEARCH.md
@.planning/phases/04-ai-integration/04-PATTERNS.md
@.planning/phases/04-ai-integration/04-UI-SPEC.md
@.planning/phases/04-ai-integration/04-VALIDATION.md
@.planning/phases/04-ai-integration/04-01-PLAN.md
@.planning/phases/04-ai-integration/04-02-PLAN.md

<interfaces>
- Requires Plan 04-01 professional BaZi fields for element/day-master signals.
- Requires Plan 04-02 term hints and knowledge links for explanatory labels.
- Existing annual `/jieri/{scene}/{year}` pages must remain server-rendered SEO pages.
- First implementation should be deterministic and local; do not add AI API routes or persistence.
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Opaque score feels arbitrary or manipulative | high | Every score dimension returns maxScore, score, reasons, and cautions. |
| Personalized birth data is persisted unintentionally | high | Keep first flow client-side/local and do not add storage, API logging, or database writes. |
| Scene role validation lets incomplete people produce false precision | medium | Scene config marks required/optional roles and tests validation. |
| Existing SEO jieri pages regress | high | Reuse candidate logic and add tests/source assertions for old annual pages. |
| Recommendations are interpreted as life-critical advice | medium | Copy and results use cultural-reference and caution language. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scene role config and scoring types</name>
  <files>
    src/lib/almanac/types.ts,
    src/lib/content/jieri-scenes.ts,
    tests/almanac/auspicious-scoring.test.ts
  </files>
  <read_first>
    src/lib/almanac/types.ts,
    src/lib/content/jieri-scenes.ts,
    src/lib/almanac/auspicious.ts,
    tests/almanac/auspicious.test.ts,
    .planning/phases/04-ai-integration/04-CONTEXT.md,
    .planning/phases/04-ai-integration/04-RESEARCH.md
  </read_first>
  <action>
1. Add exported types for `AuspiciousPersonRole`, `AuspiciousPersonInput`, `AuspiciousScoreDimension`, `AuspiciousScoreResult`, `AuspiciousRecommendationInput`, and `AuspiciousRecommendationResult`.
2. Extend or associate each `JieriSceneRule` with `personRoles`.
3. Configure exact roles:
   - `jiehun` and `hehun`: required `primary` and required `partner`;
   - `banjia`: required `primary`, optional `household`;
   - `kaiye` and `qianyue`: required `responsiblePerson`;
   - other scenes: optional `primary`.
4. Role inputs must support `birthDate`, `birthTime`, `cityId`, `gender`, and optional display `label`.
5. Add tests that assert the role configuration for `jiehun`, `banjia`, `kaiye`, and `qianyue`.
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/auspicious-scoring.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/almanac/types.ts` exports `AuspiciousScoreDimension`
    - `src/lib/almanac/types.ts` exports `AuspiciousRecommendationResult`
    - `jieriScenes` entry `jiehun` has two required roles
    - `jieriScenes` entry `banjia` has optional household role
    - `npx vitest run tests/almanac/auspicious-scoring.test.ts --reporter=verbose` exits 0
  </acceptance_criteria>
  <done>Scenes know what people data is required before scoring starts.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic score service</name>
  <files>
    src/lib/almanac/auspicious-scoring.ts,
    src/lib/almanac/auspicious.ts,
    tests/almanac/auspicious-scoring.test.ts
  </files>
  <read_first>
    src/lib/almanac/auspicious.ts,
    src/lib/almanac/service.ts,
    src/lib/almanac/bazi.ts,
    src/lib/almanac/types.ts,
    tests/almanac/auspicious.test.ts,
    tests/almanac/bazi.test.ts,
    .planning/phases/04-ai-integration/04-RESEARCH.md
  </read_first>
  <action>
1. Create `src/lib/almanac/auspicious-scoring.ts`.
2. Export `scoreAuspiciousDateRange(input)` and `scoreAuspiciousDate(input)` using deterministic inputs only.
3. Date range scoring must validate legal dates and reject ranges longer than 90 days with a clear error.
4. Score dimensions must use this exact max-score allocation: almanac fit 30, scene fit 20, zodiac/chong-sha 20, BaZi/five-element fit 20, usable lucky hours 10.
5. Use existing almanac scene matching for almanac/scene/chong-sha dimensions.
6. Use `calculateBazi` for each supplied person and compare date day stem/branch/element signals against person chart element-strength signals for a first-version explainable BaZi/five-element fit.
7. Use day/hour data from existing tyme4ts patterns to return at least zero or more usable lucky-hour labels and reasons.
8. Return ranked results descending by score, with `grade` values `excellent`, `good`, `usable`, or `caution`.
9. Every dimension must include non-empty `reasons` or `cautions`.
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/auspicious-scoring.test.ts tests/almanac/auspicious.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `auspicious-scoring.ts` exports `scoreAuspiciousDateRange`
    - Score result includes dimensions named `almanac`, `scene`, `zodiac`, `bazi`, and `luckyHours`
    - Dimension max scores are exactly `30`, `20`, `20`, `20`, `10`
    - Test asserts ranked output is sorted descending by `score`
    - Test asserts every dimension has a reason or caution
    - Existing `tests/almanac/auspicious.test.ts` still exits 0
  </acceptance_criteria>
  <done>Personalized auspicious dates can be scored and ranked with explainable dimensions.</done>
</task>

<task type="auto">
  <name>Task 3: Interactive recommendation route and client flow</name>
  <files>
    src/components/jieri/AuspiciousRecommendationForm.tsx,
    src/components/jieri/AuspiciousRecommendationResult.tsx,
    src/components/jieri/ScoreBreakdown.tsx,
    src/app/[locale]/tools/jieri-recommend/page.tsx,
    src/lib/content/tools.ts,
    tests/seo/jieri-recommend-route.test.ts
  </files>
  <read_first>
    src/components/tools/BaziForm.tsx,
    src/components/jieri/JieriScenePage.tsx,
    src/components/jieri/JieriDateRow.tsx,
    src/components/ui/button.tsx,
    src/components/ui/badge.tsx,
    src/components/ui/tabs.tsx,
    src/lib/almanac/auspicious-scoring.ts,
    src/lib/content/jieri-scenes.ts,
    src/lib/content/tools.ts,
    src/app/[locale]/tools/bazi/page.tsx,
    .planning/phases/04-ai-integration/04-UI-SPEC.md
  </read_first>
  <action>
1. Create `/[locale]/tools/jieri-recommend/page.tsx` as a Server Component with `generateMetadata`, visible SEO body copy, FAQ, internal links, and WebApplication plus FAQ JSON-LD.
2. Create `AuspiciousRecommendationForm.tsx` as a client component implementing the flow: choose scene, enter people, choose date range, submit.
3. Form must dynamically show role inputs from scene config. Marriage/matching shows two required people; moving shows primary plus optional household; business/signing/opening shows responsible person/legal representative.
4. Create `AuspiciousRecommendationResult.tsx` showing top ranked dates with score, grade, reasons, cautions, and usable lucky hours.
5. Create `ScoreBreakdown.tsx` showing the five dimensions with maxScore, score, and explanation rows.
6. Add `tests/seo/jieri-recommend-route.test.ts` source assertions for route metadata, WebApplication JSON-LD, scene flow labels, and score breakdown component usage.
  </action>
  <verify>
    <automated>npx vitest run tests/seo/jieri-recommend-route.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/[locale]/tools/jieri-recommend/page.tsx` exists
    - Page source contains `generateMetadata`
    - `AuspiciousRecommendationForm.tsx` exports `AuspiciousRecommendationForm`
    - `AuspiciousRecommendationResult.tsx` exports `AuspiciousRecommendationResult`
    - `ScoreBreakdown.tsx` exports `ScoreBreakdown`
    - Source test asserts labels for scene, people, date range, and recommendation result
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Users can complete the personalized recommendation flow from a locale-aware tool route.</done>
</task>

<task type="auto">
  <name>Task 4: Link annual jieri pages and sitemap into the flow</name>
  <files>
    src/components/jieri/JieriScenePage.tsx,
    src/lib/content/tools.ts,
    src/lib/content/registry.ts,
    src/app/sitemap.ts,
    src/app/[locale]/sitemap.ts,
    tests/seo/jieri-recommend-route.test.ts,
    tests/seo/sitemap.test.ts
  </files>
  <read_first>
    src/components/jieri/JieriScenePage.tsx,
    src/app/[locale]/jieri/[scene]/[year]/page.tsx,
    src/lib/content/registry.ts,
    src/app/sitemap.ts,
    src/app/[locale]/sitemap.ts,
    tests/seo/sitemap.test.ts,
    tests/seo/jieri-routes.test.ts
  </read_first>
  <action>
1. Add a visible CTA or compact tool link from `JieriScenePage` to `/tools/jieri-recommend?scene={scene.slug}`.
2. Preserve existing annual results, filters, metrics, FAQs, and month sections.
3. Register the new tool page in `src/lib/content/tools.ts` and `src/lib/content/registry.ts`.
4. Include `/tools/jieri-recommend` in sitemap candidates for both locales.
5. Add tests asserting the new route is registered and existing jieri route tests still pass.
  </action>
  <verify>
    <automated>npx vitest run tests/seo/jieri-recommend-route.test.ts tests/seo/jieri-routes.test.ts tests/seo/sitemap.test.ts --reporter=verbose && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `JieriScenePage.tsx` contains `/tools/jieri-recommend`
    - Registry includes `/tools/jieri-recommend`
    - Sitemap test includes `/tools/jieri-recommend`
    - Existing `tests/seo/jieri-routes.test.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Existing SEO jieri pages remain intact and route users into personalized recommendations.</done>
</task>

</tasks>

<verification>
Run after all tasks:
- `npx vitest run tests/almanac/auspicious-scoring.test.ts tests/almanac/auspicious.test.ts tests/seo/jieri-recommend-route.test.ts tests/seo/jieri-routes.test.ts tests/seo/sitemap.test.ts --reporter=verbose`
- `npx tsc --noEmit`
- `npm run build`
</verification>

<success_criteria>
- Users can choose a scene, enter scene-specific people, choose a date range, and receive ranked dates.
- Results include overall score/grade, five score dimensions, reasons, cautions, and usable lucky hours.
- Existing annual jieri pages remain SEO-rendered and link into the personalized flow.
- No AI route, persistence, or opaque scoring is introduced.
</success_criteria>
