---
phase: 04-ai-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/content/types.ts
  - src/lib/content/metaphysics.ts
  - src/lib/content/glossary.ts
  - src/lib/content/registry.ts
  - src/lib/seo.ts
  - src/components/knowledge/TermHint.tsx
  - src/components/knowledge/GlossaryPanel.tsx
  - src/components/knowledge/KnowledgeEntryCard.tsx
  - src/app/[locale]/knowledge/page.tsx
  - src/app/[locale]/knowledge/[slug]/page.tsx
  - src/app/sitemap.ts
  - src/app/[locale]/sitemap.ts
  - tests/content/metaphysics.test.ts
  - tests/seo/knowledge-routes.test.ts
autonomous: true
requirements:
  - META-03
  - META-04

must_haves:
  truths:
    - "D-03 Product inspiration is richer consumer metaphysics depth, not copying AI chat"
    - "D-16 Knowledge base is a broad metaphysics encyclopedia, not a small glossary"
    - "D-17 First-version entries include name, category, short explanation, practical use, related terms, tool appearances, mythology/cultural story, star/personality metaphor, and common misunderstandings"
    - "D-18 Coverage begins with BaZi and scoring terms while preparing for Zi Wei Dou Shu, twenty-eight mansions, Zhou Tian Xing Dou, and star deities"
    - "D-19 Tool pages integrate through term tooltip/popover hints first"
    - "D-20 Mythology and fun/story content lives primarily in standalone encyclopedia/story pages"
    - "D-21 Keep cultural research / folklore reference framing"
    - "D-22 Prefer tendency/reference/worth noting/more suitable/use with caution language"
    - "D-23 Explain methodology visibly through term detail and source notes"
  artifacts:
    - path: "src/lib/content/metaphysics.ts"
      provides: "Story-rich metaphysics taxonomy"
      contains: "metaphysicsEntries"
    - path: "src/components/knowledge/TermHint.tsx"
      provides: "In-tool term hint/popover integration"
      contains: "TermHint"
    - path: "src/app/[locale]/knowledge/[slug]/page.tsx"
      provides: "Standalone knowledge/story entry page"
      contains: "generateStaticParams"
  key_links:
    - from: "src/lib/content/glossary.ts"
      to: "src/lib/content/metaphysics.ts"
      via: "compatibility wrappers"
      pattern: "getMetaphysicsEntry"
    - from: "src/app/[locale]/knowledge/[slug]/page.tsx"
      to: "src/lib/content/registry.ts"
      via: "metadata and sitemap registration"
      pattern: "knowledge"
---

<objective>
Build the metaphysics knowledge foundation that tools can reference through rich term hints and standalone story/encyclopedia pages.

Purpose: Users can understand terms such as day master, ten gods, hidden stems, na-yin, five elements, chong-sha, and twenty-eight mansions without interrupting tool workflows.
Output: typed knowledge taxonomy, `/knowledge` routes, richer `TermHint`, registry/sitemap integration, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-integration/04-CONTEXT.md
@.planning/phases/04-ai-integration/04-RESEARCH.md
@.planning/phases/04-ai-integration/04-PATTERNS.md
@.planning/phases/04-ai-integration/04-UI-SPEC.md
@.planning/phases/04-ai-integration/04-VALIDATION.md

<interfaces>
- Keep existing `getGlossaryEntry` and `getGlossaryEntries` exports working for current pages.
- Add knowledge routes under `/[locale]/knowledge` and `/[locale]/knowledge/[slug]`.
- Prefer typed static content first; do not add Prisma schema or seed changes in this plan.
- Use existing `localizeBodyCopy`, `localizeSeo`, `buildSeoPageMetadata`, and `buildPageJsonLd` patterns.
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Knowledge content becomes untyped page blobs and cannot feed tools or AI later | high | Store entries in `src/lib/content/metaphysics.ts` with typed required fields and tests. |
| Existing almanac pages break because glossary API changes | high | Keep compatibility wrappers and test legacy keys. |
| Story/mythology content interrupts tool workflows | medium | Tool hints remain compact; long stories live on `/knowledge/[slug]`. |
| Content makes deterministic or high-stakes claims | medium | Tests check banned deterministic terms and entries include common misunderstandings/source notes. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Typed metaphysics taxonomy and glossary compatibility</name>
  <files>
    src/lib/content/types.ts,
    src/lib/content/metaphysics.ts,
    src/lib/content/glossary.ts,
    tests/content/metaphysics.test.ts
  </files>
  <read_first>
    src/lib/content/types.ts,
    src/lib/content/glossary.ts,
    src/lib/content/localize.ts,
    src/lib/content/registry.ts,
    tests/seo/content-registry.test.ts,
    .planning/phases/04-ai-integration/04-RESEARCH.md,
    .planning/phases/04-ai-integration/04-CONTEXT.md
  </read_first>
  <action>
1. Add `knowledge` to `ContentFamily` in `src/lib/content/types.ts`.
2. Add `MetaphysicsCategory`, `MetaphysicsEntry`, and any required localized/SEO helper types needed by `src/lib/content/metaphysics.ts`.
3. Create `src/lib/content/metaphysics.ts` exporting `metaphysicsEntries`, `getMetaphysicsEntry(slug, locale?)`, `getMetaphysicsEntries`, and category helpers.
4. First entries must include slugs: `four-pillars`, `day-master`, `ten-gods`, `hidden-stems`, `na-yin`, `five-elements`, `chong-sha`, `yi-ji`, `lucky-hour`, `twenty-eight-mansions`, `zi-wei-dou-shu`, and `zhou-tian-xing-dou`.
5. Every entry must include `name`, `category`, `short`, `detail`, `practicalUse`, `relatedTerms`, `toolAppearances`, `mythologyStory`, `commonMisunderstandings`, and `sourceNotes`.
6. Refactor `src/lib/content/glossary.ts` so existing `GlossaryKey` lookups are backed by or mapped to metaphysics entries while keeping the old exported function names and return shape.
7. Add `tests/content/metaphysics.test.ts` assertions for required slugs, required fields, glossary compatibility, no empty related terms for BaZi/scoring entries, and no deterministic claim words `必定`, `一定`, `保证`, `保證`.
  </action>
  <verify>
    <automated>npx vitest run tests/content/metaphysics.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/content/types.ts` includes content family `knowledge`
    - `src/lib/content/metaphysics.ts` exports `metaphysicsEntries`
    - `metaphysicsEntries` contains slug `day-master`
    - `metaphysicsEntries` contains slug `zhou-tian-xing-dou`
    - `getGlossaryEntry('ganZhi', 'zh-hant')` still returns an object with `term`, `short`, `detail`
    - `npx vitest run tests/content/metaphysics.test.ts --reporter=verbose` exits 0
  </acceptance_criteria>
  <done>Metaphysics entries are typed, test-covered, and compatible with existing glossary consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Rich term hints and knowledge panels</name>
  <files>
    src/components/knowledge/TermHint.tsx,
    src/components/knowledge/GlossaryPanel.tsx,
    src/components/knowledge/KnowledgeEntryCard.tsx,
    tests/content/metaphysics.test.ts
  </files>
  <read_first>
    src/components/knowledge/TermHint.tsx,
    src/components/knowledge/GlossaryPanel.tsx,
    src/components/ui/tooltip.tsx,
    src/components/ui/badge.tsx,
    src/components/ui/button.tsx,
    src/lib/content/glossary.ts,
    src/lib/content/metaphysics.ts,
    .planning/phases/04-ai-integration/04-UI-SPEC.md
  </read_first>
  <action>
1. Extend `TermHint` so it can render optional richer fields from a metaphysics entry: `chartHint`, `practicalUse`, `sourceNotes`, and `href`.
2. Keep the trigger accessible with `aria-label` containing the term and short definition.
3. The hint body must stay compact: term, short definition, optional chart-specific hint, optional source-note line, and one link to the full knowledge page.
4. Update `GlossaryPanel` to render richer entry details without nesting card components inside other cards.
5. Add `KnowledgeEntryCard.tsx` for `/knowledge` index cards with stable layout, category label, short text, and link.
6. Add source-level tests or assertions in `tests/content/metaphysics.test.ts` verifying `TermHint.tsx` references `sourceNotes` or equivalent and links to `/knowledge`.
  </action>
  <verify>
    <automated>npx vitest run tests/content/metaphysics.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `TermHint.tsx` still exports `TermHint`
    - `TermHint.tsx` contains `aria-label`
    - `TermHint.tsx` renders a link when `href` is present
    - `KnowledgeEntryCard.tsx` exports `KnowledgeEntryCard`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Tool term hints can expose richer explanations and link into standalone knowledge pages.</done>
</task>

<task type="auto">
  <name>Task 3: Knowledge index and entry routes</name>
  <files>
    src/app/[locale]/knowledge/page.tsx,
    src/app/[locale]/knowledge/[slug]/page.tsx,
    src/lib/content/registry.ts,
    src/lib/seo.ts,
    tests/seo/knowledge-routes.test.ts
  </files>
  <read_first>
    src/app/[locale]/tools/page.tsx,
    src/app/[locale]/feng-shui/[category]/[slug]/page.tsx,
    src/components/seo/SeoPageShell.tsx,
    src/components/seo/SeoHero.tsx,
    src/components/seo/FaqBlock.tsx,
    src/components/seo/InternalLinkGrid.tsx,
    src/lib/content/metaphysics.ts,
    src/lib/content/registry.ts,
    src/lib/seo.ts,
    src/i18n/routing.ts
  </read_first>
  <action>
1. Create `/[locale]/knowledge/page.tsx` as a Server Component with `generateMetadata`, H1, category filters/sections, entry cards, FAQ, internal links, and JSON-LD.
2. Create `/[locale]/knowledge/[slug]/page.tsx` with `generateStaticParams`, `generateMetadata`, 404 handling for unknown slugs, breadcrumbs, H1, short definition, practical use, story/cultural context, common misunderstandings, related terms, tool appearances, and JSON-LD.
3. Register knowledge index and entry pages in `src/lib/content/registry.ts`.
4. If `src/lib/seo.ts` needs an article/page helper extension for knowledge entries, add it centrally there rather than assembling JSON-LD ad hoc in route files.
5. Add `tests/seo/knowledge-routes.test.ts` to assert route files exist, `generateStaticParams` covers `day-master`, metadata uses the central helper, and JSON-LD includes Article or WebPage plus BreadcrumbList.
  </action>
  <verify>
    <automated>npx vitest run tests/seo/knowledge-routes.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/[locale]/knowledge/page.tsx` exists
    - `src/app/[locale]/knowledge/[slug]/page.tsx` exists
    - Knowledge entry route source contains `generateStaticParams`
    - Registry includes `/knowledge`
    - Test asserts slug `day-master`
    - `npx vitest run tests/seo/knowledge-routes.test.ts --reporter=verbose` exits 0
  </acceptance_criteria>
  <done>Knowledge pages are locale-aware, SEO-rendered, registered, and test-covered.</done>
</task>

<task type="auto">
  <name>Task 4: Sitemap and route coverage</name>
  <files>
    src/app/sitemap.ts,
    src/app/[locale]/sitemap.ts,
    src/lib/content/registry.ts,
    tests/seo/knowledge-routes.test.ts
  </files>
  <read_first>
    src/app/sitemap.ts,
    src/app/[locale]/sitemap.ts,
    src/lib/content/registry.ts,
    tests/seo/sitemap.test.ts,
    tests/seo/metadata-coverage.test.ts
  </read_first>
  <action>
1. Include `/knowledge` and every `metaphysicsEntries` detail page in sitemap candidates for both locales.
2. Keep existing jieri/zodiac/feng-shui/tool sitemap behavior unchanged.
3. Add tests that assert `/zh-hant/knowledge`, `/zh-hans/knowledge/day-master`, and `/zh-hant/knowledge/zhou-tian-xing-dou` appear in sitemap or registry-derived sitemap candidates.
4. Do not include any AI route in sitemap because Phase 4 AI is deferred.
  </action>
  <verify>
    <automated>npx vitest run tests/seo/knowledge-routes.test.ts tests/seo/sitemap.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Sitemap candidates include `/knowledge`
    - Sitemap candidates include `/knowledge/day-master`
    - Existing `tests/seo/sitemap.test.ts` still exits 0
    - No new route path contains `/ai`
  </acceptance_criteria>
  <done>Knowledge pages are discoverable without disrupting existing SEO route families.</done>
</task>

</tasks>

<verification>
Run after all tasks:
- `npx vitest run tests/content/metaphysics.test.ts tests/seo/knowledge-routes.test.ts tests/seo/sitemap.test.ts --reporter=verbose`
- `npx tsc --noEmit`
- `npm run build`
</verification>

<success_criteria>
- Typed metaphysics encyclopedia entries exist with story and practical-use fields.
- Existing glossary consumers continue to work.
- `/knowledge` and `/knowledge/[slug]` are server-rendered and SEO-registered.
- Term hints can link tool labels to knowledge pages.
- Content stays culturally framed and avoids deterministic claims.
</success_criteria>
