---
phase: 02-core-almanac-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02 # file conflict guard: both plans modify types.ts, cache.ts, service.ts, messages/*.json — sequential execution required
files_modified:
  - src/lib/almanac/types.ts
  - src/lib/almanac/cache.ts
  - src/lib/almanac/service.ts
  - src/i18n/messages/zh-hant.json
  - src/i18n/messages/zh-hans.json
  - src/components/almanac/SolarTermsList.tsx
  - src/app/[locale]/solar-terms/page.tsx
autonomous: true
requirements:
  - ALM-06

must_haves:
  truths:
    - "User navigates to /[locale]/solar-terms and sees all 24 solar terms grouped by season (春/夏/秋/冬)"
    - "Each solar term shows its name, date, jie/qi classification, meaning, and traditional customs"
    - "Solar terms page has a title '二十四節氣' and seasonal group headings"
    - "Solar term dates are computed dynamically from tyme4ts; meanings and customs are static i18n content"
    - "Page handles errors gracefully with translated error messages"
  artifacts:
    - path: "src/lib/almanac/types.ts"
      provides: "SolarTerm interface"
      contains: "interface SolarTerm"
    - path: "src/lib/almanac/service.ts"
      provides: "getSolarTerms method"
      contains: "getSolarTerms"
    - path: "src/lib/almanac/cache.ts"
      provides: "Solar terms cache helpers"
      contains: "getCachedSolarTerms"
    - path: "src/components/almanac/SolarTermsList.tsx"
      provides: "24 solar terms list grouped by season"
      min_lines: 40
    - path: "src/app/[locale]/solar-terms/page.tsx"
      provides: "Solar terms page"
      contains: "getSolarTerms"
  key_links:
    - from: "src/app/[locale]/solar-terms/page.tsx"
      to: "src/components/almanac/SolarTermsList.tsx"
      via: "import and render with terms prop"
      pattern: "import.*SolarTermsList"
    - from: "src/app/[locale]/solar-terms/page.tsx"
      to: "src/lib/almanac/service.ts"
      via: "getSolarTerms call"
      pattern: "getSolarTerms"
    - from: "src/lib/almanac/service.ts"
      to: "src/lib/almanac/cache.ts"
      via: "getCachedSolarTerms"
      pattern: "getCachedSolarTerms"
---

<objective>
Build the 24 solar terms page with terms computed from tyme4ts, grouped by season (spring/summer/autumn/winter), showing each term's name, date, jie/qi classification, meaning, and traditional customs.

Purpose: Users can look up all 24 jie-qi with dates, meanings, and customs for the current year, organized by traditional seasonal groupings.
Output: Working solar terms page at /[locale]/solar-terms with seasonal grouping, dynamic dates, and static cultural content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-almanac-ui/02-CONTEXT.md
@.planning/phases/02-core-almanac-ui/02-RESEARCH.md
@.planning/phases/02-core-almanac-ui/02-PATTERNS.md
@.planning/phases/02-core-almanac-ui/02-UI-SPEC.md

<interfaces>
<!-- From Plan 01 + 02 output -- these will exist after Plans 01 and 02 execute -->

From src/lib/almanac/types.ts (new SolarTerm to add; HourlyFortune and CalendarDay already added by Plans 01/02):
```typescript
export interface HourlyFortune { name: string; ganZhi: string; star: string; fortune: '吉' | '凶'; yi: string[]; ji: string[]; }
export interface CalendarDay { solarDay: number; lunarDay: string; fortune: '吉' | '凶'; isToday: boolean; dateStr: string; weekday: number; }
// NEW to add:
export interface SolarTerm { name: string; date: string; isJie: boolean; year: number; }
```

From src/lib/almanac/service.ts (existing + Plan 01/02 additions):
```typescript
export async function getDailyAlmanac(dateStr: string): Promise<DailyAlmanac> { ... }
export async function getHourlyFortune(dateStr: string): Promise<HourlyFortune[]> { ... }
export async function getMonthlyCalendar(year: number, month: number): Promise<CalendarDay[]> { ... }
```

From src/app/[locale]/page.tsx (existing error handling pattern):
```typescript
if (error || !almanac) {
  const tHome = await getTranslations('Homepage');
  return (<Card className="max-w-prose w-full">...</Card>);
}
```

From src/i18n/navigation.ts:
```typescript
export const { Link, redirect, usePathname, useRouter, getPathname } = createNavigation(routing);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SolarTerm type + getSolarTerms service + i18n keys</name>
  <files>
    src/lib/almanac/types.ts, src/lib/almanac/cache.ts, src/lib/almanac/service.ts,
    src/i18n/messages/zh-hant.json, src/i18n/messages/zh-hans.json
  </files>
  <read_first>
    src/lib/almanac/types.ts, src/lib/almanac/cache.ts, src/lib/almanac/service.ts,
    src/i18n/messages/zh-hant.json, src/i18n/messages/zh-hans.json,
    .planning/phases/02-core-almanac-ui/02-RESEARCH.md,
    .planning/phases/02-core-almanac-ui/02-PATTERNS.md
  </read_first>
  <action>
1. Extend `src/lib/almanac/types.ts` -- add SolarTerm interface after CalendarDay:
   - `export interface SolarTerm { name: string; date: string; isJie: boolean; year: number; }`

2. Extend `src/lib/almanac/cache.ts` -- add solar terms cache helpers:
   - `getCachedSolarTerms(year: number)` / `setCachedSolarTerms(year: number, data: SolarTerm[])` with key `almanac:terms:${year}`, TTL 31536000 (365 days)
   - Import SolarTerm type from ./types

3. Extend `src/lib/almanac/service.ts` -- add getSolarTerms method:
   - Add `Term` to the tyme4ts import (alongside existing SolarDay, SolarMonth, LunarHour)
   - Add imports for getCachedSolarTerms, setCachedSolarTerms from ./cache
   - Add import for SolarTerm type from ./types
   - Method signature: `export async function getSolarTerms(year: number): Promise<SolarTerm[]>`
   - Implementation per RESEARCH.md Pattern 3:
     - Check cache first
     - Loop i from 0 to 23: `Term.fromIndex(year, i)`
     - For each term: get name via t.toString(), get date from t.getSolarDay() (format YYYY-MM-DD), get isJie via t.isJie(), get year via t.getYear()
     - CRITICAL: Index 0 returns previous year's 冬至. Filter by `t.getYear() === year` to only include terms for the requested year.
     - Cache result, return

4. Extend i18n messages -- add SolarTerms namespace with term meanings and customs.
   In `src/i18n/messages/zh-hant.json`, add "SolarTerms" namespace:
   - title: "二十四節氣"
   - spring: "春季"
   - summer: "夏季"
   - autumn: "秋季"
   - winter: "冬季"
   - jieLabel: "節"
   - qiLabel: "氣"
   - dateMonth: "月"
   - dateDay: "日"
   - meaningLabel: "含義"
   - customsLabel: "傳統習俗"
   - error.heading: "節氣資料載入失敗"
   - error.body: "請稍後再試，或返回首頁。"
   - terms object (keyed by term name, each with meaning and customs):
     - 立春: meaning="春季開始，萬物復蘇", customs="咬春、打春牛、貼春聯"
     - 雨水: meaning="降雨開始，雨量增多", customs="回娘屋、拉保保"
     - 驚蟄: meaning="春雷驚醒冬眠動物", customs="打小人、祭白虎"
     - 春分: meaning="晝夜平分，春季過半", customs="豎蛋、吃春菜"
     - 清明: meaning="天氣清澈明朗", customs="掃墓祭祖、踏青、插柳"
     - 穀雨: meaning="雨生百穀，降雨增多", customs="賞牡丹、飲穀雨茶"
     - 立夏: meaning="夏季開始", customs="嘗新、鬥蛋、稱人"
     - 小滿: meaning="麥類等作物籽粒開始飽滿", customs="祭車神、蠶神"
     - 芒種: meaning="有芒的作物開始成熟", customs="送花神、煮梅"
     - 夏至: meaning="白晝最長，陽氣最盛", customs="吃夏至麵、祭地"
     - 小暑: meaning="天氣開始炎熱", customs="曬書畫、吃藕"
     - 大暑: meaning="一年中最熱時期", customs="飲伏茶、曬伏薑"
     - 立秋: meaning="秋季開始", customs="貼秋膘、啃秋瓜"
     - 處暑: meaning="暑氣結束", customs="放河燈、開漁節"
     - 白露: meaning="天氣轉涼，露水凝結", customs="收清露、飲白露茶"
     - 秋分: meaning="晝夜平分，秋季過半", customs="豎蛋、吃秋菜"
     - 寒露: meaning="露水更涼，將要結霜", customs="登高賞菊、飲菊花酒"
     - 霜降: meaning="天氣漸冷，開始有霜", customs="吃柿子、進補"
     - 立冬: meaning="冬季開始", customs="補冬、吃餃子"
     - 小雪: meaning="開始降雪，雪量不大", customs="醃臘肉、吃糍粑"
     - 大雪: meaning="降雪增多，範圍更廣", customs="進補、賞雪"
     - 冬至: meaning="白晝最短，陽氣始生", customs="吃湯圓、吃餃子、祭祖"
     - 小寒: meaning="天氣寒冷但未到極點", customs="吃臘八粥、寫春聯"
     - 大寒: meaning="一年中最冷時期", customs="尾牙祭、除舊迎新"

   In `src/i18n/messages/zh-hans.json`, add "SolarTerms" namespace:
   - title: "二十四节气"
   - spring: "春季"
   - summer: "夏季"
   - autumn: "秋季"
   - winter: "冬季"
   - jieLabel: "节"
   - qiLabel: "气"
   - dateMonth: "月"
   - dateDay: "日"
   - meaningLabel: "含义"
   - customsLabel: "传统习俗"
   - error.heading: "节气数据加载失败"
   - error.body: "请稍后重试，或返回首页。"
   - terms object (same 24 entries with simplified Chinese equivalents)
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/almanac/types.ts exports SolarTerm interface with fields: name (string), date (string), isJie (boolean), year (number)
    - src/lib/almanac/cache.ts exports getCachedSolarTerms and setCachedSolarTerms using key "almanac:terms:{year}" with TTL 31536000
    - src/lib/almanac/service.ts exports getSolarTerms(year: number) returning Promise<SolarTerm[]>; uses Term from tyme4ts; loops Term.fromIndex(year, 0..23)
    - src/i18n/messages/zh-hant.json contains "SolarTerms" namespace with title "二十四節氣", spring/summer/autumn/winter keys, and terms object with 24 entries each having meaning and customs
    - src/i18n/messages/zh-hans.json contains "SolarTerms" namespace with title "二十四节气" and matching structure
  </acceptance_criteria>
  <done>SolarTerm type defined; getSolarTerms method added to AlmanacService with 365-day Redis cache; SolarTerms i18n namespace present in both locale files</done>
</task>

<task type="auto">
  <name>Task 2: SolarTermsList component + SolarTerms page</name>
  <files>
    src/components/almanac/SolarTermsList.tsx,
    src/app/[locale]/solar-terms/page.tsx
  </files>
  <read_first>
    src/app/[locale]/page.tsx, src/app/[locale]/layout.tsx,
    src/styles/globals.css, src/lib/utils.ts,
    .planning/phases/02-core-almanac-ui/02-UI-SPEC.md,
    .planning/phases/02-core-almanac-ui/02-PATTERNS.md
  </read_first>
  <action>
1. Create `src/components/almanac/SolarTermsList.tsx`:
   - Props: `{ terms: SolarTerm[] }` (import SolarTerm from @/lib/almanac/types)
   - Use getTranslations('SolarTerms') for labels
   - Server Component
   - Structure per UI-SPEC D-12:
     - Group terms by season using month from term.date:
       - month 2-4 → spring (春), 5-7 → summer (夏), 8-10 → autumn (秋), 11-12 or 1 → winter (冬)
     - Each season group: a Card (shadcn) with:
       - CardHeader: season name (e.g., "春季")
       - CardContent: list of terms, each row showing:
         - Top row: Term name (large, text-lg font-semibold) + Date ("{month}月{day}日") + Jie/Qi badge ("節"/"氣")
         - Bottom row: Meaning text (text-sm, muted) from i18n terms[termName].meaning
         - Third row: Customs text (text-sm) from i18n terms[termName].customs, prefixed with "習俗：" label
         - Layout: vertical stack within each term block
     - Use Separator between term rows within each card
     - Access meaning/customs via `t('terms.${termName}.meaning')` and `t('terms.${termName}.customs')` pattern
   - Container: `max-w-2xl` to match other pages

2. Create `src/app/[locale]/solar-terms/page.tsx`:
   - Server Component (no searchParams needed -- shows current year's terms)
   - Import getSolarTerms from @/lib/almanac/service
   - Import SolarTermsList from @/components/almanac/SolarTermsList
   - Import getTranslations from next-intl/server
   - Import Card, CardHeader, CardTitle, CardContent from @/components/ui/card
   - Data fetching:
     - `const currentYear = new Date().getFullYear()`
     - `const terms = await getSolarTerms(currentYear)`
   - Error handling: same pattern as homepage (Card with translated error)
   - Render: `<SolarTermsList terms={terms} />`
   - generateMetadata for SEO:
     - title from SolarTerms i18n (e.g., "二十四節氣")
     - description mentioning the year and number of terms
   - Page title: h1 with translated title
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/almanac/SolarTermsList.tsx accepts SolarTerm[] props, groups terms by season (spring/summer/autumn/winter), renders 4 Card components with season headings
    - Each term row shows term name, date in "月日" format, and jie/qi Badge label
    - src/app/[locale]/solar-terms/page.tsx is a Server Component, calls getSolarTerms(currentYear), renders SolarTermsList, has generateMetadata
    - Page title displays "二十四節氣" (zh-hant) or "二十四节气" (zh-hans)
    - Error state renders when getSolarTerms fails with translated error messages
    - `npx next build` succeeds without errors
  </acceptance_criteria>
  <done>Solar terms page at /[locale]/solar-terms shows all 24 jie-qi grouped by season with dates, jie/qi classification, meanings, and traditional customs; dates computed dynamically from tyme4ts, cultural content from static i18n data with 365-day Redis cache</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tyme4ts output -> React rendering | Library output rendered as text content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Year param in service | mitigate | getSolarTerms validates year is a positive integer; tyme4ts throws for invalid ranges |
| T-02-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- /[locale]/solar-terms shows 24 solar terms grouped into 4 season cards
- Each term shows name, date, jie/qi badge, meaning, and traditional customs
- Season headings display correctly (春季/夏季/秋季/冬季)
- Page title shows "二十四節氣"
- Term meanings and customs render from i18n content in both locales
- Error state renders if getSolarTerms fails
- Both locales render correctly
</verification>

<success_criteria>
- getSolarTerms returns SolarTerm[] with 24 terms for the requested year
- SolarTermsList groups terms into 4 seasonal Cards
- Each term displays name, date, isJie classification, meaning, and customs
- zh-hant.json and zh-hans.json contain SolarTerms.terms with 24 entries each having meaning and customs
- Page has SEO metadata via generateMetadata
- Both zh-hant and zh-hans render correct seasonal labels and cultural content
</success_criteria>

<output>
Create `.planning/phases/02-core-almanac-ui/02-03-SUMMARY.md` when done
</output>
