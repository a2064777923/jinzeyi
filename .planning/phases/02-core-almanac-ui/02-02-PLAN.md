---
phase: 02-core-almanac-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/almanac/types.ts
  - src/lib/almanac/cache.ts
  - src/lib/almanac/service.ts
  - src/i18n/messages/zh-hant.json
  - src/i18n/messages/zh-hans.json
  - src/components/almanac/CalendarDayCell.tsx
  - src/components/almanac/MonthlyCalendar.tsx
  - src/app/[locale]/calendar/page.tsx
autonomous: true
requirements:
  - ALM-05

must_haves:
  truths:
    - "User navigates to /[locale]/calendar and sees a monthly calendar grid with 7 columns (日一二三四五六)"
    - "Each day cell shows solar day number, lunar day name, and fortune color coding (red/gold = 吉, gray = 凶)"
    - "Today's cell has a highlighted border"
    - "User clicks left/right arrows to switch months; URL updates to ?month=YYYY-MM"
    - "User clicks a day cell and navigates to /[locale]/almanac/YYYY-MM-DD"
    - "Calendar page shows month title: '2026年5月 · 農曆四月'"
  artifacts:
    - path: "src/lib/almanac/types.ts"
      provides: "CalendarDay interface"
      contains: "interface CalendarDay"
    - path: "src/lib/almanac/service.ts"
      provides: "getMonthlyCalendar method"
      contains: "getMonthlyCalendar"
    - path: "src/lib/almanac/cache.ts"
      provides: "Monthly calendar cache helpers"
      contains: "getCachedMonthlyCalendar"
    - path: "src/components/almanac/CalendarDayCell.tsx"
      provides: "Individual calendar day cell"
      min_lines: 25
    - path: "src/components/almanac/MonthlyCalendar.tsx"
      provides: "Monthly calendar grid with navigation"
      min_lines: 50
    - path: "src/app/[locale]/calendar/page.tsx"
      provides: "Calendar page with searchParams month switching"
      contains: "getMonthlyCalendar"
  key_links:
    - from: "src/app/[locale]/calendar/page.tsx"
      to: "src/components/almanac/MonthlyCalendar.tsx"
      via: "import and render with days prop"
      pattern: "import.*MonthlyCalendar"
    - from: "src/components/almanac/MonthlyCalendar.tsx"
      to: "src/components/almanac/CalendarDayCell.tsx"
      via: "render day cells in grid"
      pattern: "CalendarDayCell"
    - from: "src/app/[locale]/calendar/page.tsx"
      to: "src/lib/almanac/service.ts"
      via: "getMonthlyCalendar call"
      pattern: "getMonthlyCalendar"
    - from: "src/lib/almanac/service.ts"
      to: "src/lib/almanac/cache.ts"
      via: "getCachedMonthlyCalendar"
      pattern: "getCachedMonthlyCalendar"
    - from: "src/components/almanac/CalendarDayCell.tsx"
      to: "src/i18n/navigation.ts"
      via: "Link to detail page"
      pattern: "import.*Link.*i18n/navigation"
---

<objective>
Build the monthly calendar view with a 7-column grid showing each day's lunar date and fortune color coding, month navigation via URL searchParams, and clickable day cells linking to the detail page.

Purpose: Users can browse any month's daily fortune at a glance and drill into individual days for full almanac detail.
Output: Working calendar page at /[locale]/calendar with month navigation and day-to-detail-page linking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-almanac-ui/02-CONTEXT.md
@.planning/phases/02-core-almanac-ui/02-RESEARCH.md
@.planning/phases/02-core-almanac-ui/02-PATTERNS.md
@.planning/phases/02-core-almanac-ui/02-UI-SPEC.md

<interfaces>
<!-- From Plan 01 output -- these will exist after Plan 01 executes -->

From src/lib/almanac/types.ts (new CalendarDay to add):
```typescript
export interface CalendarDay {
  solarDay: number;      // 1-31
  lunarDay: string;      // 初一, 十五, ...
  fortune: '吉' | '凶';  // binary from tyme4ts (no '平')
  isToday: boolean;
  dateStr: string;       // YYYY-MM-DD for linking
  weekday: number;       // 0=Sun, 6=Sat
}
```

From src/lib/almanac/service.ts (existing getDailyAlmanac pattern):
```typescript
import { SolarDay } from 'tyme4ts';
import { getCachedAlmanac, setCachedAlmanac } from './cache';
import type { DailyAlmanac } from './types';
export async function getDailyAlmanac(dateStr: string): Promise<DailyAlmanac> { ... }
```

From src/lib/almanac/cache.ts (existing pattern):
```typescript
import { redis } from '@/lib/redis';
const CACHE_TTL = 86400;
export async function getCachedAlmanac(dateStr: string): Promise<DailyAlmanac | null> { ... }
export async function setCachedAlmanac(dateStr: string, data: DailyAlmanac): Promise<void> { ... }
```

From src/app/[locale]/page.tsx (existing error handling pattern):
```typescript
if (error || !almanac) {
  const tHome = await getTranslations('Homepage');
  return (<Card className="max-w-prose w-full">...</Card>);
}
```

From src/i18n/navigation.ts:
```typescript
export const { Link, redirect, usePathname, useRouter, getPathname } = createNavigation(routing);
```

From src/styles/globals.css:
- --color-primary: #C43B3B (red - for 吉日 bg, today border)
- --color-gold: #B8860B (for 吉日 lunar text)
- --color-muted: #F0EBE3 (for 凶日 bg)
- --color-muted-foreground: #6B6B6B (for 凶日 text)
- --color-card: #F5F0E8
- --color-border: #E5DFD5
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalendarDay + getMonthlyCalendar service + i18n keys</name>
  <files>
    src/lib/almanac/types.ts, src/lib/almanac/cache.ts, src/lib/almanac/service.ts,
    src/i18n/messages/zh-hant.json, src/i18n/messages/zh-hans.json
  </files>
  <read_first>
    src/lib/almanac/types.ts, src/lib/almanac/cache.ts, src/lib/almanac/service.ts,
    src/i18n/messages/zh-hant.json, src/i18n/messages/zh-hans.json,
    .planning/phases/02-core-almanac-ui/02-RESEARCH.md,
    .planning/phases/02-core-almanac-ui/02-PATTERNS.md
  </read_first>
  <action>
1. Extend `src/lib/almanac/types.ts` -- add CalendarDay interface after HourlyFortune:
   - `export interface CalendarDay { solarDay: number; lunarDay: string; fortune: '吉' | '凶'; isToday: boolean; dateStr: string; weekday: number; }`
   - Note: fortune is binary ('吉' | '凶') per RESEARCH.md Pitfall 3 -- tyme4ts does not return '平'

2. Extend `src/lib/almanac/cache.ts` -- add monthly calendar cache helpers following existing pattern:
   - `getCachedMonthlyCalendar(cacheKey: string)` / `setCachedMonthlyCalendar(cacheKey: string, data: CalendarDay[])` with key pattern `almanac:monthly:{cacheKey}`, TTL 604800 (7 days)
   - Import CalendarDay type from ./types

3. Extend `src/lib/almanac/service.ts` -- add getMonthlyCalendar method:
   - Add `SolarMonth` to the tyme4ts import
   - Add imports for getCachedMonthlyCalendar, setCachedMonthlyCalendar from ./cache
   - Add import for CalendarDay type from ./types
   - Method signature: `export async function getMonthlyCalendar(year: number, month: number): Promise<CalendarDay[]>`
   - Implementation per RESEARCH.md Pattern 2:
     - Build cacheKey as `${year}-${String(month).padStart(2, '0')}`
     - Check cache first
     - Use SolarMonth.fromYm(year, month).getDays() to get all days
     - Compute todayStr using same formatToday() logic (inline helper or extract to shared util)
     - For each day: get lunar day name via d.getLunarDay().getName(), get fortune via d.getLunarDay().getTwelveStar().getEcliptic().getLuck().toString() as '吉' | '凶', get weekday via d.getWeek().index
     - Build dateStr as `${d.getYear()}-${String(d.getMonth()).padStart(2, '0')}-${String(d.getDay()).padStart(2, '0')}`
     - Set isToday by comparing dateStr === todayStr
     - Cache result, return

4. Extend i18n messages -- add Calendar namespace.
   In `src/i18n/messages/zh-hant.json`, add "Calendar" namespace:
   - title: "月曆"
   - monthFormat: "{year}年{month}月 · 農曆{lunarMonth}" (ICU syntax)
   - weekdays: ["日", "一", "二", "三", "四", "五", "六"]
   - today: "今天"
   - prevMonth: "上個月"
   - nextMonth: "下個月"
   - legend.auspicious: "紅底 = 吉日"
   - legend.inauspicious: "灰底 = 凶日"
   - error.heading: "無法載入月曆資料"
   - error.body: "請稍後再試，或返回首頁。"

   In `src/i18n/messages/zh-hans.json`, add "Calendar" namespace:
   - title: "月历"
   - monthFormat: "{year}年{month}月 · 农历{lunarMonth}"
   - weekdays: ["日", "一", "二", "三", "四", "五", "六"]
   - today: "今天"
   - prevMonth: "上个月"
   - nextMonth: "下个月"
   - legend.auspicious: "红底 = 吉日"
   - legend.inauspicious: "灰底 = 凶日"
   - error.heading: "无法加载月历数据"
   - error.body: "请稍后重试，或返回首页。"
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/almanac/types.ts exports CalendarDay interface with fields: solarDay (number), lunarDay (string), fortune ('吉' | '凶'), isToday (boolean), dateStr (string), weekday (number)
    - src/lib/almanac/cache.ts exports getCachedMonthlyCalendar and setCachedMonthlyCalendar using key pattern "almanac:monthly:{key}" with TTL 604800
    - src/lib/almanac/service.ts exports getMonthlyCalendar(year: number, month: number) returning Promise<CalendarDay[]>; uses SolarMonth.fromYm from tyme4ts
    - src/i18n/messages/zh-hant.json contains "Calendar" namespace with title "月曆" and weekdays array
    - src/i18n/messages/zh-hans.json contains "Calendar" namespace with title "月历" and weekdays array
    - Both message files have Calendar.legend.auspicious and Calendar.legend.inauspicious keys
  </acceptance_criteria>
  <done>CalendarDay type defined; getMonthlyCalendar method added to AlmanacService with 7-day Redis cache; Calendar i18n namespace present in both locale files</done>
</task>

<task type="auto">
  <name>Task 2: CalendarDayCell + MonthlyCalendar + CalendarPage</name>
  <files>
    src/components/almanac/CalendarDayCell.tsx,
    src/components/almanac/MonthlyCalendar.tsx,
    src/app/[locale]/calendar/page.tsx
  </files>
  <read_first>
    src/app/[locale]/page.tsx, src/app/[locale]/layout.tsx,
    src/i18n/navigation.ts, src/styles/globals.css, src/lib/utils.ts,
    .planning/phases/02-core-almanac-ui/02-UI-SPEC.md,
    .planning/phases/02-core-almanac-ui/02-PATTERNS.md
  </read_first>
  <action>
1. Create `src/components/almanac/CalendarDayCell.tsx`:
   - Props: `{ day: CalendarDay }` (import CalendarDay from @/lib/almanac/types)
   - Use getTranslations('Calendar') for "today" label (client component needed for useTranslations)
   - Actually, make it a Server Component -- pass the "today" label as a prop or use getTranslations
   - Better approach: make it a simple presentational component receiving day data + a `todayLabel` string prop
   - Cell structure per UI-SPEC D-06/D-07:
     - Wrapper: Link from @/i18n/navigation with href={`/almanac/${day.dateStr}`}
     - Cell size: min-h-[48px] (per UI-SPEC spacing), p-1 or p-2
     - Fortune color coding:
       - 吉日: `bg-primary text-primary-foreground` (red bg, white text for solar number)
       - 凶日: `bg-muted text-muted-foreground` (gray bg)
     - Today indicator: `ring-2 ring-primary` border (per UI-SPEC) regardless of fortune
     - Content: solar day number (text-base font-semibold) + lunar day name (text-[12px] font-normal per UI-SPEC one-off utility)
     - For 吉日 cells: lunar day text in gold `text-gold` per UI-SPEC color coding
   - If day.isToday, show "今天" label below the cell (small text)

2. Create `src/components/almanac/MonthlyCalendar.tsx`:
   - Props: `{ year: number; month: number; days: CalendarDay[] }`
   - Use getTranslations('Calendar') for headers and labels
   - Server Component
   - Structure per UI-SPEC layout contract:
     - Page title: h1 "月曆"
     - Month navigation bar:
       - Left arrow button (shadcn Button variant="outline", size="icon") linking to `?month=${prevMonth}`
       - Center text: month format from i18n (e.g., "2026年5月 · 農曆四月")
         - For lunar month name: use the first day's lunar data to get the lunar month (days[0].lunarDay context) -- OR compute via tyme4ts SolarDay.fromYmd(year, month, 1).getLunarDay().getLunarMonth().getName()
         - Since this is a Server Component, import SolarDay from tyme4ts to get lunar month name
       - Right arrow button linking to `?month=${nextMonth}`
     - Calendar grid: CSS Grid with `grid-cols-7 gap-1`
     - Weekday header row: 7 divs with text from i18n weekdays array
     - Empty cells: Array.from({ length: firstDayOffset }) for days before the 1st
       - firstDayOffset = days[0].weekday (0=Sun)
     - Day cells: map over days array, render CalendarDayCell for each
     - Legend row below grid: "紅底 = 吉日" / "灰底 = 凶日" with colored dots
   - Month navigation:
     - Previous month: compute prevYear/prevMonth (handle January -> December of previous year)
     - Next month: compute nextYear/nextMonth (handle December -> January of next year)
     - Use Link from @/i18n/navigation with href={`/calendar?month=${prevYear}-${String(prevMonth).padStart(2,'0')}`}
   - Container: `max-w-2xl` to match homepage width

3. Create `src/app/[locale]/calendar/page.tsx`:
   - Server Component with searchParams
   - Interface: `Props { searchParams: Promise<{ month?: string }> }`
   - Extract month param: parse YYYY-MM format, default to current year/month
   - Validate month format: /^\d{4}-\d{2}$/ regex, reject invalid
   - Fetch data: `const days = await getMonthlyCalendar(year, monthNum)`
   - Error handling: same pattern as homepage (Card with error message from i18n)
   - Render: `<MonthlyCalendar year={year} month={monthNum} days={days} />`
   - generateMetadata for SEO: title from Calendar i18n, description mentioning the month
   - Export generateStaticParams if desired for ISR (optional)
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/almanac/CalendarDayCell.tsx imports CalendarDay, renders Link to /almanac/{dateStr}, shows solar day number + lunar day name, applies fortune color coding (bg-primary for 吉, bg-muted for 凶), shows ring-2 ring-primary for today
    - src/components/almanac/MonthlyCalendar.tsx uses CSS Grid with grid-cols-7, renders weekday headers (日一二三四五六), renders CalendarDayCell for each day, has left/right month navigation buttons using Link, displays month title with lunar month name
    - src/app/[locale]/calendar/page.tsx is a Server Component, reads searchParams.month, calls getMonthlyCalendar, renders MonthlyCalendar, has generateMetadata
    - Calendar grid first-day offset matches weekday of the 1st (empty cells before day 1)
    - Month navigation prev/next buttons update URL to ?month=YYYY-MM
    - `npx next build` succeeds without errors
  </acceptance_criteria>
  <done>Monthly calendar page at /[locale]/calendar shows a 7-column grid with fortune color coding, month navigation via URL searchParams, clickable day cells linking to /almanac/YYYY-MM-DD, and today's cell highlighted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| searchParams.month -> date parsing | Month string from URL query param enters getMonthlyCalendar |
| tyme4ts output -> React rendering | Library output rendered as text content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | searchParams.month | mitigate | Validate month format with /^\d{4}-\d{2}$/ regex; reject invalid values with error state |
| T-02-05 | Denial of Service | Extreme month ranges | mitigate | getMonthlyCalendar internally validates year/month range via tyme4ts (throws for invalid dates) |
| T-02-SC | Tampering | npm installs | accept | No new packages in this plan; shadcn components installed in Plan 01 |
</threat_model>

<verification>
- /[locale]/calendar shows monthly grid with correct day count for the month
- Each day cell shows solar number + lunar day + fortune color
- Today cell has highlighted border
- Left/right arrows switch months and update URL
- Clicking a day navigates to /[locale]/almanac/YYYY-MM-DD
- Legend shows "紅底 = 吉日" / "灰底 = 凶日"
- Month title shows "2026年5月 · 農曆四月" format
- Error state renders when getMonthlyCalendar fails
</verification>

<success_criteria>
- Calendar grid renders correct number of days for any month
- Fortune color coding: 吉日 = red bg, 凶日 = gray bg
- Today cell has primary border highlight
- Month navigation works in both directions with URL persistence
- Day cell click navigates to detail page
- Month title includes lunar month name
- Both zh-hant and zh-hans render correctly
</success_criteria>

<output>
Create `.planning/phases/02-core-almanac-ui/02-02-SUMMARY.md` when done
</output>
