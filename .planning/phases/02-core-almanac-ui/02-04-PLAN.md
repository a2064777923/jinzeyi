---
phase: 02-core-almanac-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - src/i18n/messages/zh-hant.json
  - src/i18n/messages/zh-hans.json
  - src/components/almanac/AlmanacDetail.tsx
  - src/app/[locale]/almanac/[date]/page.tsx
autonomous: true
requirements:
  - ALM-07

must_haves:
  truths:
    - "User navigates to /[locale]/almanac/YYYY-MM-DD and sees full almanac detail with tabbed sections"
    - "Detail page has 5 tabs: 概覽, 宜忌, 時辰, 方位, 神煞"
    - "Default tab is 概覽; tab state is URL-based (?tab=hours) for deep linking"
    - "Invalid date format shows 404 page"
    - "Page has SEO metadata (title with date, description with yi/ji preview)"
    - "Navigation links: 返回月曆 and 回到今天"
    - "Hourly fortune tab reuses HourlyFortuneTable component from Plan 01"
  artifacts:
    - path: "src/components/almanac/AlmanacDetail.tsx"
      provides: "Tabbed detail page component"
      min_lines: 80
    - path: "src/app/[locale]/almanac/[date]/page.tsx"
      provides: "Daily almanac detail page with SSR and generateMetadata"
      contains: "generateMetadata"
  key_links:
    - from: "src/app/[locale]/almanac/[date]/page.tsx"
      to: "src/components/almanac/AlmanacDetail.tsx"
      via: "import and render"
      pattern: "import.*AlmanacDetail"
    - from: "src/app/[locale]/almanac/[date]/page.tsx"
      to: "src/lib/almanac/service.ts"
      via: "getDailyAlmanac + getHourlyFortune calls"
      pattern: "getDailyAlmanac|getHourlyFortune"
    - from: "src/components/almanac/AlmanacDetail.tsx"
      to: "src/components/almanac/HourlyFortuneTable.tsx"
      via: "import and render in hours tab"
      pattern: "import.*HourlyFortuneTable"
    - from: "src/components/almanac/AlmanacDetail.tsx"
      to: "src/components/almanac/YiJiBadgeList.tsx"
      via: "import and render in yi-ji tab"
      pattern: "import.*YiJiBadgeList"
    - from: "src/components/almanac/AlmanacDetail.tsx"
      to: "src/i18n/navigation.ts"
      via: "Link to calendar and today"
      pattern: "import.*Link.*i18n/navigation"
---

<objective>
Build the daily almanac detail page at /[locale]/almanac/YYYY-MM-DD with a 5-tab layout (概覽/宜忌/時辰/方位/神煞), URL-based tab state for deep linking, SSR rendering with SEO metadata, and navigation links back to calendar and today.

Purpose: Users can access complete almanac information for any date via a shareable URL, with organized sections for easy browsing.
Output: Working detail page at /[locale]/almanac/YYYY-MM-DD with tabbed layout, SEO metadata, and 404 handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-almanac-ui/02-CONTEXT.md
@.planning/phases/02-core-almanac-ui/02-RESEARCH.md
@.planning/phases/02-core-almanac-ui/02-PATTERNS.md
@.planning/phases/02-core-almanac-ui/02-UI-SPEC.md
@.planning/phases/02-core-almanac-ui/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -- these exist after Plan 01 executes -->

From src/lib/almanac/types.ts:
```typescript
export interface DailyAlmanac {
  solar: { year: number; month: number; day: number };
  lunar: { year: string; month: string; day: string; lunarDate: string };
  ganZhi: { year: string; month: string; day: string };
  zodiac: string;
  yi: string[];
  ji: string[];
  direction: { chong: string; sha: string; caiShen: string; xiShen: string; fuShen: string };
  gods: string[];
  duty: string;
  twentyEightStar: string;
  pengZu: string;
  sound: string;
  fetusDay: string;
}
export interface HourlyFortune {
  name: string;
  ganZhi: string;
  star: string;
  fortune: '吉' | '凶';
  yi: string[];
  ji: string[];
}
```

From src/lib/almanac/service.ts:
```typescript
export async function getDailyAlmanac(dateStr: string): Promise<DailyAlmanac> { ... }
export async function getHourlyFortune(dateStr: string): Promise<HourlyFortune[]> { ... }
```

From src/components/almanac/HourlyFortuneTable.tsx (Plan 01 output):
```typescript
export function HourlyFortuneTable({ hours }: { hours: HourlyFortune[] }) { ... }
```

From src/components/almanac/YiJiBadgeList.tsx (Plan 01 output):
```typescript
export function YiJiBadgeList({ items, type }: { items: string[]; type: 'yi' | 'ji' }) { ... }
```

From src/components/almanac/FortuneMarker.tsx (Plan 01 output):
```typescript
export function FortuneMarker({ fortune, size }: { fortune: '吉' | '凶'; size?: 'sm' | 'lg' }) { ... }
```

From src/app/[locale]/layout.tsx:
```typescript
export default async function LocaleLayout({ children, params }: { children: React.ReactNode; params: Promise<{ locale: string }> }) { ... }
// main wrapper: max-w-[65ch] w-full px-4 py-8
```

From src/i18n/navigation.ts:
```typescript
export const { Link, redirect, usePathname, useRouter, getPathname } = createNavigation(routing);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detail i18n keys</name>
  <files>
    src/i18n/messages/zh-hant.json, src/i18n/messages/zh-hans.json
  </files>
  <read_first>
    src/i18n/messages/zh-hant.json, src/i18n/messages/zh-hans.json,
    .planning/phases/02-core-almanac-ui/02-UI-SPEC.md
  </read_first>
  <action>
Add "Detail" namespace to both i18n message files per UI-SPEC copywriting contract.

In `src/i18n/messages/zh-hant.json`, add "Detail" namespace:
- titleFormat: "{year}年{month}月{day}日 黃曆" (ICU syntax)
- tab.overview: "概覽"
- tab.yiJi: "宜忌"
- tab.hours: "時辰"
- tab.directions: "方位"
- tab.deities: "神煞"
- backToCalendar: "返回月曆"
- backToToday: "回到今天"
- notFound.heading: "找不到該日黃曆"
- notFound.body: "請檢查日期是否正確，或返回月曆瀏覽。"
- error.heading: "黃曆資料載入失敗"
- error.body: "請稍後再試，或返回月曆瀏覽。"
- overview.lunarDate: "農曆"
- overview.ganZhi: "干支"
- overview.zodiac: "生肖"
- overview.chongSha: "沖煞"
- directions.caiShen: "財神位"
- directions.xiShen: "喜神位"
- directions.fuShen: "福神位"
- directions.chong: "沖"
- directions.sha: "煞"
- deities.gods: "吉神凶煞"
- deities.duty: "值神"
- deities.twentyEightStar: "二十八星宿"
- deities.pengZu: "彭祖百忌"
- deities.sound: "納音"
- deities.fetusDay: "胎神"

In `src/i18n/messages/zh-hans.json`, add "Detail" namespace:
- titleFormat: "{year}年{month}月{day}日 黄历"
- tab.overview: "概览"
- tab.yiJi: "宜忌"
- tab.hours: "时辰"
- tab.directions: "方位"
- tab.deities: "神煞"
- backToCalendar: "返回月历"
- backToToday: "回到今天"
- notFound.heading: "找不到该日黄历"
- notFound.body: "请检查日期是否正确，或返回月历浏览。"
- error.heading: "黄历数据加载失败"
- error.body: "请稍后重试，或返回月历浏览。"
- overview.lunarDate: "农历"
- overview.ganZhi: "干支"
- overview.zodiac: "生肖"
- overview.chongSha: "冲煞"
- directions.caiShen: "财神位"
- directions.xiShen: "喜神位"
- directions.fuShen: "福神位"
- directions.chong: "冲"
- directions.sha: "煞"
- deities.gods: "吉神凶煞"
- deities.duty: "值神"
- deities.twentyEightStar: "二十八星宿"
- deities.pengZu: "彭祖百忌"
- deities.sound: "纳音"
- deities.fetusDay: "胎神"
  </action>
  <verify>
    <automated>grep -c "Detail" src/i18n/messages/zh-hant.json && grep -c "Detail" src/i18n/messages/zh-hans.json</automated>
  </verify>
  <acceptance_criteria>
    - src/i18n/messages/zh-hant.json contains "Detail" namespace with tab.overview "概覽", tab.hours "時辰", backToCalendar "返回月曆", notFound.heading "找不到該日黃曆"
    - src/i18n/messages/zh-hans.json contains "Detail" namespace with tab.overview "概览", tab.hours "时辰", backToCalendar "返回月历", notFound.heading "找不到该日黄历"
    - Both files have all 5 tab keys (overview, yiJi, hours, directions, deities)
    - Both files have deities.gods, deities.duty, deities.twentyEightStar, deities.pengZu, deities.sound, deities.fetusDay keys
  </acceptance_criteria>
  <done>Detail namespace with all tab labels, navigation labels, error messages, and section headings present in both zh-hant and zh-hans message files</done>
</task>

<task type="auto">
  <name>Task 2: AlmanacDetail component + Detail page with SSR and SEO</name>
  <files>
    src/components/almanac/AlmanacDetail.tsx,
    src/app/[locale]/almanac/[date]/page.tsx
  </files>
  <read_first>
    src/app/[locale]/page.tsx, src/app/[locale]/layout.tsx,
    src/components/layout/Header.tsx,
    src/components/almanac/HourlyFortuneTable.tsx,
    src/components/almanac/YiJiBadgeList.tsx,
    src/components/almanac/FortuneMarker.tsx,
    src/styles/globals.css, src/lib/utils.ts,
    .planning/phases/02-core-almanac-ui/02-UI-SPEC.md,
    .planning/phases/02-core-almanac-ui/02-PATTERNS.md
  </read_first>
  <action>
1. Create `src/components/almanac/AlmanacDetail.tsx`:
   - Props: `{ almanac: DailyAlmanac; hours: HourlyFortune[]; activeTab: string }`
   - Import DailyAlmanac, HourlyFortune from @/lib/almanac/types
   - Import Tabs, TabsContent, TabsList, TabsTrigger from @/components/ui/tabs
   - Import Card, CardContent, CardHeader, CardTitle from @/components/ui/card
   - Import Separator from @/components/ui/separator
   - Import Badge from @/components/ui/badge
   - Import HourlyFortuneTable from ./HourlyFortuneTable
   - Import YiJiBadgeList from ./YiJiBadgeList
   - Import Link from @/i18n/navigation
   - Import getTranslations from next-intl/server (Server Component)
   - Use getTranslations('Detail') for all labels

   - Navigation bar at top (per UI-SPEC):
     - "返回月曆" Link: href="/calendar" (preserves locale via next-intl Link)
     - "回到今天" Link: href={`/almanac/${todayStr}` where todayStr is computed inline

   - Page title: h1 with format "{year}年{month}月{day}日 黃曆"

   - Tabs component with defaultValue={activeTab} (from props, comes from URL searchParams):
     - TabsList with 5 triggers: overview, yiJi, hours, directions, deities
     - Each TabsTrigger value matches the tab key (e.g., value="overview")

   - TabsContent for "overview" (概覽):
     - Lunar date: almanac.lunar.lunarDate
     - Gan-zhi: "{almanac.ganZhi.year}年 {almanac.ganZhi.month}月 {almanac.ganZhi.day}日"
     - Zodiac: almanac.zodiac
     - Chong-sha: "沖{almanac.direction.chong} 煞{almanac.direction.sha}"
     - Yi summary: YiJiBadgeList with almanac.yi (type="yi")
     - Ji summary: YiJiBadgeList with almanac.ji (type="ji")

   - TabsContent for "yiJi" (宜忌):
     - Full yi list with YiJiBadgeList (type="yi")
     - Full ji list with YiJiBadgeList (type="ji")
     - Separator between yi and ji sections

   - TabsContent for "hours" (時辰):
     - HourlyFortuneTable with hours prop

   - TabsContent for "directions" (方位):
     - Chong-sha: "沖{almanac.direction.chong} 煞{almanac.direction.sha}"
     - Cai-shen: almanac.direction.caiShen
     - Xi-shen: almanac.direction.xiShen
     - Fu-shen: almanac.direction.fuShen
     - Layout: 2x2 grid or vertical list with labels

   - TabsContent for "deities" (神煞):
     - Gods: almanac.gods joined with "、"
     - Duty: almanac.duty
     - Twenty-eight star: almanac.twentyEightStar
     - Peng-zu: almanac.pengZu
     - Sound: almanac.sound
     - Fetus day: almanac.fetusDay
     - Each field: label from i18n + value, separated by Separator

   - Container: `max-w-3xl` per UI-SPEC (wider than homepage to accommodate tabs and tables)

2. Create `src/app/[locale]/almanac/[date]/page.tsx`:
   - Server Component with dynamic route
   - Interface: `Props { params: Promise<{ locale: string; date: string }>; searchParams: Promise<{ tab?: string }> }`
   - Import notFound from next/navigation
   - Import getDailyAlmanac, getHourlyFortune from @/lib/almanac/service
   - Import AlmanacDetail from @/components/almanac/AlmanacDetail
   - Import getTranslations from next-intl/server
   - Import Card, CardHeader, CardTitle, CardContent from @/components/ui/card
   - Import Link from @/i18n/navigation

   - Date validation (per RESEARCH.md Pattern 4):
     - Extract date from params
     - Validate format: `/^\d{4}-\d{2}-\d{2}$/` regex; call notFound() if invalid
     - Parse year/month/day from date string
     - Validate range: year >= 1900 && year <= 2100; month 1-12; day 1-31 (basic check)

   - Data fetching:
     - Fetch both almanac and hourly data: `const [almanac, hours] = await Promise.all([getDailyAlmanac(date), getHourlyFortune(date)])`
     - Wrap in try/catch; on error, render error card with translated message

   - Tab state from URL:
     - Read searchParams for tab
     - Valid tab values: 'overview', 'yiJi', 'hours', 'directions', 'deities'
     - Default to 'overview' if not specified or invalid
     - Pass activeTab to AlmanacDetail component

   - Error handling:
     - Invalid date format: notFound()
     - Data fetch error: render error Card with translated message + "返回月曆" link

   - generateMetadata:
     - Title: use Detail.titleFormat with almanac data (e.g., "2026年5月17日 黃曆")
     - Description: "今日宜：{yi.slice(0,3).join('、')}。忌：{ji.slice(0,3).join('、')}。"
     - If date is invalid or fetch fails, return basic title

   - Render: `<AlmanacDetail almanac={almanac} hours={hours} activeTab={activeTab} />`
  </action>
  <verify>
    <automated>npx vitest run tests/almanac/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/almanac/AlmanacDetail.tsx renders shadcn Tabs with 5 TabsTrigger (overview/yiJi/hours/directions/deities) and 5 TabsContent sections
    - Overview tab shows lunar date, gan-zhi, zodiac, chong-sha, yi/ji summary
    - YiJi tab shows full yi and ji lists using YiJiBadgeList component
    - Hours tab renders HourlyFortuneTable component with the hours prop
    - Directions tab shows chong-sha, cai-shen, xi-shen, fu-shen
    - Deities tab shows gods, duty, twenty-eight star, peng-zu, sound, fetus day
    - "返回月曆" link points to /calendar; "回到今天" link points to /almanac/{todayStr}
    - src/app/[locale]/almanac/[date]/page.tsx validates date format with regex, calls notFound() for invalid dates
    - Page fetches both getDailyAlmanac and getHourlyFortune via Promise.all
    - generateMetadata returns title with date and description with yi/ji preview
    - Tab state defaults to 'overview' when no ?tab= searchParam
    - Container uses max-w-3xl
    - `npx next build` succeeds without errors
  </acceptance_criteria>
  <done>Detail page at /[locale]/almanac/YYYY-MM-DD renders full almanac info in 5 tabs (概覽/宜忌/時辰/方位/神煞) with URL-based tab state, SEO metadata, 404 for invalid dates, and navigation links back to calendar and today</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param [date] -> date parsing | Date string from dynamic route segment enters AlmanacService |
| URL searchParams.tab -> tab state | Tab parameter from URL controls which tab is displayed |
| tyme4ts output -> React rendering | Library output rendered as text content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | URL [date] param | mitigate | Validate date format with /^\d{4}-\d{2}-\d{2}$/ regex; call notFound() for invalid; validate year range 1900-2100 |
| T-02-08 | Tampering | searchParams.tab | mitigate | Whitelist valid tab values ('overview','yiJi','hours','directions','deities'); default to 'overview' for invalid values |
| T-02-09 | Information Disclosure | tyme4ts output | accept | React auto-escapes; tyme4ts returns plain strings |
| T-02-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- /[locale]/almanac/2026-05-17 renders full almanac detail with 5 tabs
- Default tab is 概覽 showing lunar date, gan-zhi, zodiac, chong-sha, yi/ji
- Clicking 時辰 tab shows hourly fortune table
- URL changes to ?tab=hours when clicking the tab
- Direct URL /[locale]/almanac/2026-05-17?tab=directions opens to 方位 tab
- /[locale]/almanac/invalid-date returns 404
- "返回月曆" navigates to calendar page
- "回到今天" navigates to today's detail page
- generateMetadata produces correct title and description
- Both locales render correctly
</verification>

<success_criteria>
- Detail page renders 5 tabs with correct content in each
- Tab state is URL-based and deep-linkable
- Invalid dates return 404 via notFound()
- SEO metadata includes date in title and yi/ji in description
- Navigation links work correctly
- HourlyFortuneTable and YiJiBadgeList are reused from Plan 01
- Both zh-hant and zh-hans render all section labels correctly
</success_criteria>

<output>
Create `.planning/phases/02-core-almanac-ui/02-04-SUMMARY.md` when done
</output>
