import { beforeAll, afterAll } from 'vitest';

// Shared test fixtures and setup
beforeAll(() => {
  // Any global test setup can go here
});

afterAll(() => {
  // Any global test teardown can go here
});
